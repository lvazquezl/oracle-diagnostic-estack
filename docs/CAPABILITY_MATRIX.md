# Capability Matrix

Vista legible de [`config/capability-matrix.yaml`](../config/capability-matrix.yaml), que es la **fuente estructurada de verdad**. Este Markdown debe mantenerse consistente con el YAML — ver `tests/test_capability_matrix_registry_consistency.sh`. Consumida por el gate `version`/`architecture` de todo Workflow Contract (`docs/CONTRACTS.md#workflow-contract`) y por `policies/version-awareness-policy.md`.

## Leyenda

| Status | Significado |
|---|---|
| `PLANNED` | La feature Oracle existe en esa versión, pero el e-stack aún no tiene contrato/skill/query ni siquiera registrado para ella. |
| `FOUNDATION_ONLY` | El dominio tiene agente/contrato/skills `registered`, pero ningún skill materializado (`status: active`) cubre esta versión todavía. |
| `PARTIAL` | Al menos un skill materializado cubre esta versión, pero no el dominio completo. |
| `SUPPORTED` | Cobertura completa certificada (no ocurre aún en Fase 1/Hardening, salvo `/change` como proceso). |
| `UNSUPPORTED` | La feature Oracle **no existe** en esa versión/arquitectura — no es una limitación del e-stack. |
| `LICENSE_DEPENDENT` | Depende de licenciamiento adicional (Diagnostics/Tuning Pack, etc.) independientemente de la cobertura del e-stack — ver `policies/licensing-awareness-policy.md`. |

`latest` trackea la versión mayor soportada más reciente (hoy: 23ai) hasta que se agregue una nueva vía `/change compatibility`.

## Matriz

| Dominio | 10g | 11g | 12c | 18c | 19c | 21c | 23ai | latest |
|---|---|---|---|---|---|---|---|---|
| Oracle Core | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED |
| Performance | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED |
| AWR | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT |
| ASH | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT |
| ADDM | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT | LICENSE_DEPENDENT |
| Statspack | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED |
| RAC | PLANNED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED |
| GI | UNSUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED |
| ASM | UNSUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED |
| Multitenant | UNSUPPORTED | UNSUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | COMPATIBILITY_VALIDATION_REQUIRED |
| Data Guard | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | COMPATIBILITY_VALIDATION_REQUIRED |
| RMAN / Backup & Recovery | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | COMPATIBILITY_VALIDATION_REQUIRED |
| Security & Compliance | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | COMPATIBILITY_VALIDATION_REQUIRED |
| Network | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED |
| OS | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | COMPATIBILITY_VALIDATION_REQUIRED |
| Capacity | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | COMPATIBILITY_VALIDATION_REQUIRED |
| Incident Analysis & RCA | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | COMPATIBILITY_VALIDATION_REQUIRED |
| Documentation | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL |
| /change | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED |
| MCP Diagnostic Gateway | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL |

## Notas por dominio

- **Oracle Core**: `SUPPORTED` desde Fase 2 — las 18 skills `oracle/*` completamente materializadas (`SKILL.md` + `manifest.yaml`), ~20 queries `Q-ORA-*` certificadas (Query Contract v2), 40 tests dedicados. `oracle/diagnostics` (ADR) requiere 11g+; en 10g esa sub-capacidad específica degrada a `UNSUPPORTED` sin afectar el resto del dominio.
- **Performance**: `SUPPORTED` desde Fase 3 — 31 skills `performance/*` completamente materializadas (`SKILL.md` + `manifest.yaml`), catálogo `queries/performance/**` (21 queries certificadas, Query Contract v2 + Query Variant Contract). Esta fila mide el core no licenciado (ruta estándar sin Diagnostics Pack, Statspack); AWR/ASH/ADDM son capacidades `LICENSE_DEPENDENT` independientes, filas propias abajo.
- **AWR/ASH/ADDM**: requieren Diagnostics Pack en todas las versiones donde existen (10g+) — `LICENSE_DEPENDENT` no es una limitación del e-stack sino del contrato de licenciamiento del cliente; ver `policies/licensing-awareness-policy.md`. Los 3 skills (`performance/awr-analysis`, `performance/ash-analysis`, `performance/addm-analysis`) están completamente materializados y funcionan cuando la licencia se confirma.
- **Statspack**: `SUPPORTED` desde Fase 3, cobertura multi-sección completa desde Fase 3 Completion Hardening — no requiere licencia adicional; `skills/performance/statspack-analysis` cubre Load Profile, Instance Efficiency, Top Wait Events, SQL ordered by CPU/elapsed/executions/gets/reads, Instance Activity, Library Cache, Latch, Enqueue, I/O (incl. ASM), Memory/Cache Sizes, Redo/Commit y Parsing derivados, vía consulta en vivo (`Q-PERF-WAIT-STATSPACK-001`, sólo wait events) y vía ingesta de reporte texto (`parsers/performance/statspack_parser.py`). Cobertura por-sección es `SUPPORTED`/`PARTIALLY_SUPPORTED`/`UNSUPPORTED` según el reporte concreto suministrado — ver `docs/PHASE_3_COMPLETION_HARDENING.md#gap-1--statspack-completion`.
- **RAC**: `SUPPORTED` desde Fase 4 (11gR2–23ai) — 19 skills `rac/*` completamente materializados (topología, servicios, session distribution, load balancing CLB/RLB, interconnect, Cache Fusion, recursos Clusterware). 10g/11gR1 (CRS legacy) no certificado → `PLANNED` (la feature Oracle sí existe), no `UNSUPPORTED`. RAC deep performance (impacto de `gc *` en DB Time) `DELEGATES_TO_PERFORMANCE`, no se duplica en este dominio. **RAC detection** básica sigue `SUPPORTED` desde Fase 2 vía el Target Profile.
- **GI (Grid Infrastructure)**: `SUPPORTED` desde Fase 4 (11gR2–23ai) — absorbido por `oracle-rac-analyst` (sin agente separado), 12 skills `rac/gi-*` materializados (version, node-status, resource-status/-properties, scan, vip, listeners, network-interfaces, ocr-status, voting-status, cluster-health, configuration-consistency). Como marca/arquitectura formal es 11gR2+ (antes: Oracle Clusterware) → `UNSUPPORTED` en 10g. OCR/Voting repair explícitamente `PROHIBITED` (sólo visibility).
- **ASM**: `SUPPORTED` desde Fase 4 (11gR2–23ai) — 12 skills `asm/*` completamente materializados (topología, diskgroups, capacidad real vía `USABLE_FILE_MB`, redundancia, salud de discos, failure groups, rebalance), reemplaza el `asm/capacity.md` plano de Foundation. 10g fuera de soporte activo (arquitectura de disk group difiere significativamente antes de 11g). ASM write operations explícitamente `PROHIBITED`. **ASM detection** básica sigue `SUPPORTED` desde Fase 2.
- **Multitenant**: `SUPPORTED` desde Fase 6 (12.1–23ai) — 26 skills `multitenant/*` completamente materializados (architecture, cdb-discovery, pdb-inventory, pdb-state, pdb-open-mode, pdb-services, pdb-rac-placement, pdb-sessions, pdb-tablespaces, pdb-temp, pdb-undo, local-undo, parameters, common-local-users, common-local-roles, components, plugin-violations, resource-usage, resource-manager, lockdown-profiles, application-containers, proxy-pdb, configuration-drift, healthcheck, assessment, troubleshooting), 15 queries `Q-CDB-*` certificadas. `Local Undo` no existe en 12.1 (introducido en 12.2) — degradado vía Query Variant Contract, nunca asumido. `Application Containers`/`Proxy PDB` (12.2+) son `PARTIALLY_SUPPORTED` (topología vía `V$PDBS`, sin profundidad de lifecycle). No existe antes de 12c → `UNSUPPORTED` real (no es una limitación del e-stack); en `NON-CDB` (cualquier versión) `MULTITENANT_STATUS: NOT_APPLICABLE` se publica directamente desde el Target Profile sin activar el agente. Más de 1 PDB por CDB es `LICENSE_DEPENDENT` según edición — señalado por finding específico, no a nivel de dominio completo. **Container detection** básica sigue `SUPPORTED` desde Fase 2 vía el Target Profile. Ninguna ejecución de `ALTER PLUGGABLE DATABASE`/`CREATE`/`DROP`/`CLONE`/`UNPLUG`/`PLUG`/`SAVE STATE`/cambio de parámetro/Resource Manager/lockdown profile/usuario común o local — sólo visibilidad y diagnóstico. **PHASE 6 — QUERY COMPATIBILITY & DICTIONARY CERTIFICATION HARDENING**: 3 sub-capacidades con precisión de versión distinta del agregado 12.1–23ai — PDB Saved State `SUPPORTED` desde **12.1.0.2** (patch level; no existe en 12.1.0.0/12.1.0.1; vista real `DBA_PDB_SAVED_STATES`, no `CDB_PDB_SAVED_STATES`, que no existe — verificado, 404 en docs.oracle.com); PDB Resource Metrics `PARTIALLY_SUPPORTED` en 12.1 / `SUPPORTED` en 12.2+ (`V$RSRCPDBMETRIC` introducida en 12.2.0.1, sin fuente alternativa certificada para 12.1); Plug-in Violations `SUPPORTED` con variantes version-specific (12.1 sin `CON_ID`, `container_id: NOT_AVAILABLE`; 12.2+ con `CON_ID`). Ver `docs/PHASE_6_QUERY_COMPATIBILITY_HARDENING.md`. **Versión futura**: mismo criterio que Data Guard Fase 5 — la columna "latest" arriba deja de decir `SUPPORTED`, declara `COMPATIBILITY_VALIDATION_REQUIRED`; `agents/oracle-multitenant-analyst/manifest.yaml#supported_versions` declara esa familia `UNKNOWN_FUTURE`, `config/capability-matrix.yaml` declara `future_status: COMPATIBILITY_VALIDATION_REQUIRED` en la fila `multitenant`, y todas las queries `Q-CDB-*` en `config/query-compatibility-matrix.yaml` usan un `max` explícito (`"23.0"`, nunca `latest`).
- **Data Guard**: `SUPPORTED` desde Fase 5 (10g–23ai) — 21 skills `dataguard/*` completamente materializados. Foco principal `PHYSICAL_STANDBY`; `LOGICAL_STANDBY`/`SNAPSHOT_STANDBY`/`FAR_SYNC` reconocidos pero `PARTIALLY_SUPPORTED` (sin análisis profundo específico en esta fase). Switchover/failover **execution** `PROHIBITED` — sólo readiness/visibility. Active Data Guard (lectura en standby) es `LICENSE_DEPENDENT`, señalado por finding específico vía `ACTIVE_DATA_GUARD_CHECK`. **Role detection** básica sigue `SUPPORTED` desde Fase 2 vía el Target Profile. **Versión futura (PHASE 5 — DATA GUARD FINAL PROCESS-VIEW & PORTABILITY HARDENING, `# 15-16`)**: la columna "latest" arriba deja de decir `SUPPORTED` — desde este hardening declara explícitamente `COMPATIBILITY_VALIDATION_REQUIRED` (no una reinterpretación por comentario, un cambio estructural real). "23ai" es la última major *certificada*; una major Oracle futura y aún desconocida nunca hereda soporte — `agents/oracle-dataguard-analyst/manifest.yaml#supported_versions` declara esa familia como `UNKNOWN_FUTURE`, `config/capability-matrix.yaml` declara `future_status: COMPATIBILITY_VALIDATION_REQUIRED` en la fila `dataguard`, y todas las queries `Q-DG-*` en `config/query-compatibility-matrix.yaml` usan un `max` explícito (`"23.0"` o `"12.1"` según variante, nunca `latest`) para que el Query Variant Resolver nunca resuelva una major desconocida como compatible por herencia. Una major nueva requiere `/change compatibility` antes de certificarse en cualquiera de estas cuatro fuentes.
- **Network**: `SUPPORTED` desde Fase 4 (10g–23ai) — 14 skills `network/*` completamente materializados (Oracle Net, listeners, SCAN, SCAN resolution, service registration, LOCAL/REMOTE_LISTENER, connection path, TNS errors, timeouts, name resolution, interconnect, healthcheck, troubleshooting), reemplaza el `network/tns.md` plano de Foundation. SCAN es exclusivo de RAC 11gR2+; el resto del dominio aplica igual en Standalone. Ningún cambio de configuración de red/DNS/firewall/listener ejecutado (`PROHIBITED`).
- **RMAN / Backup & Recovery**: `SUPPORTED` desde Fase 7 — 30 skills `rman/*` completamente materializados (`SKILL.md` + `manifest.yaml`), catálogo `queries/rman/**` (14 queries certificadas, Query Contract v2 + Query Variant Contract). RMAN execution (BACKUP/RESTORE/RECOVER/DELETE/CROSSCHECK/CHANGE/CONFIGURE/CATALOG/UNCATALOG/DUPLICATE/SWITCH DATABASE/ALLOCATE CHANNEL) explícitamente `PROHIBITED` — sólo visibilidad, readiness y runbooks manuales (`execution_status: NOT_EXECUTED`). PDB PITR/multitenant-awareness son sub-capacidades `PARTIALLY_SUPPORTED` (no certificadas al detalle completo por versión) sin afectar el resto del dominio. Reemplaza la fila `PARTIAL` de Foundation (sólo `rman/backup-status.md` materializado, plano).
- **Security & Compliance**: `SUPPORTED` desde Fase 8 (10g–23ai) — 39 skills `security/*` completamente materializados (accounts/roles/privileges/PUBLIC/admin privileges, password/profile posture, password-strength policy assessment, password verify function analysis read-only, Unified/Traditional Auditing, TDE/keystore/tablespace-encryption, network encryption/TLS awareness, security parameters, database links/directories, Database Vault/OLS/Data Redaction/Data Masking awareness, licensing gates, compliance mapping), catálogo `queries/security/**` (30 queries certificadas, Query Contract v2 + Query Variant Contract). Sub-capacidades con boundary propio degradan sin afectar el dominio completo: Unified Auditing `PARTIALLY_SUPPORTED` en 10g/11g (Traditional Auditing es la vía certificada ahí); TDE/keystore/tablespace-encryption `PARTIALLY_SUPPORTED` en 10g/11.1 (column encryption sí disponible desde 10.2); `INACTIVE_ACCOUNT_TIME` `PARTIALLY_SUPPORTED` por debajo de 12.2. Database Vault/OLS/Data Redaction/Data Masking son siempre `LICENSE_DEPENDENT` — nunca declarados usables por mera visibilidad técnica (`V$OPTION`), señalados por finding específico vía `security/licensing-gates`, no a nivel de dominio completo. Password strength assessment evalúa la **política** configurada (profiles + verify function source, read-only), nunca el contenido real de contraseñas — actual password inspection y password hash/verifier exposure explícitamente `PROHIBITED`, igual que password testing/cracking y toda mutación (`CREATE/ALTER/DROP USER|ROLE|PROFILE`, `GRANT/REVOKE`, `AUDIT/NOAUDIT`, `ADMINISTER KEY MANAGEMENT`, cambios de Database Vault/OLS/Redaction/Masking) — sólo visibilidad, assessment y manual remediation plans (`execution_status: NOT_EXECUTED`). Deepening de `agents/oracle-security-analyst.md` (manifest plano real desde Foundation, a diferencia de RMAN) a contrato estructurado completo — responsabilidades/boundaries preservadas y ampliadas, nunca contradichas. **PHASE 8 — SECURITY QUERY COMPATIBILITY, ORACLE NET EVIDENCE & STATIC VALIDATOR HARDENING**: `Q-SEC-NETWORK-ENCRYPTION-PARAMS-001` retirada (`NOT_CERTIFIED: INCORRECT_EVIDENCE_SOURCE` — SQLNET.\* nunca es evidencia de `V$PARAMETER`); el catálogo certificado pasa de 30 a 29 queries SQL, compensado por el nuevo collector semántico `get_oracle_net_security_configuration` (`network/oracle-net-security`, propiedad de `oracle-network-analyst`). Sub-capacidad `Network encryption assessment`: `PARTIALLY_SUPPORTED` — `Oracle Net semantic collector`: `CONTRACT_DEFINED / NOT_RUNTIME_CERTIFIED` (especificación certificada, Gateway MCP real es Fase 13 — roadmap vigente, igual que el resto de collectors del e-stack; sin collector runtime, `evidence_source: MANUAL_SANITIZED_ORACLE_NET_CONFIGURATION`, nunca `SUPPORTED` fingido). `Static dictionary validation`: `SUPPORTED` — el Static Validator ahora cubre objetos `DBA_*/CDB_*/ALL_*/USER_*/ROLE_*/AUDIT_*/UNIFIED_*/REDACTION_*` además de `V$/GV$` (gap real corregido, ver `docs/PHASE_8_SECURITY_QUERY_ORACLE_NET_STATIC_VALIDATOR_HARDENING.md`).
- **OS**: `SUPPORTED` desde Fase 9 — 45 skills `os/*` completamente materializados (discovery/platform-version, CPU/NUMA/memoria/swap/memory-pressure, HugePages/THP, process-limits/open-files/ulimits/systemd-limits/shared-memory/semaphores/aio/kernel-parameter-assessment, ephemeral-ports/tcp-socket-awareness/network-interfaces/bonding/vlan/mtu/routing/dns, filesystems/inodes/mount-options/block-devices/multipath-awareness/io-performance, time-sync/ssh-sshd-awareness/oracle-groups/oracle-processes/grid-processes/cgroups/log-pressure, 4 skills de integración cross-domain `os/rac-interconnect-awareness`/`os/dataguard-network-awareness`/`os/rman-media-manager-awareness`/`os/security-filesystem-awareness` por referencia, y `os/platform-healthcheck`/`os/platform-assessment`/`os/manual-hardening-plan`). No depende de la versión Oracle — `os` es capa de plataforma, la tabla lo repite igual en las columnas 10g–23ai por consistencia estructural. **Linux** (Oracle Linux/RHEL/SUSE/LinuxONE) `SUPPORTED`. **Windows Server** `SUPPORTED` con sub-capacidades `PARTIALLY_SUPPORTED`/`NOT_APPLICABLE` donde el modelo de OS difiere (`ulimits`/`hugepages`/`aio`/`mount-options` `NOT_APPLICABLE`; `ephemeral-ports`/`time-sync`/`network-interfaces`/`filesystems`/`process-limits`/`open-files` `SUPPORTED` vía collectors WMI dedicados) — ningún PowerShell arbitrario expuesto. **Solaris** `PARTIALLY_SUPPORTED` — comandos nativos propios (`psrinfo -pv`/`prtconf`/`kstat`/`swap -s`/`ipadm`/`dladm show-aggr`, terminología "aggregation" en vez de "bonding"), nunca sintaxis Linux asumida. **AIX/HP-UX** fuera de alcance de esta fase — `registered` en `skills/REGISTRY.md`, sin skill materializado. HugePages (`required_pages = ceil(total_SGA_bytes / hugepage_size_bytes)`, suma de todas las SGAs del host, sin margen fijo) y parámetros de kernel siempre evaluados contra el sizing real del host, nunca una cifra universal hardcodeada. Mutación de OS (paquetes/servicios/kernel params/`sysctl -w`/ulimits/usuarios-grupos/permisos/firewall/red/bonding/VLAN/mount/LVM-ZFS-VxVM/SELinux-AppArmor/sshd config/Windows Registry/Windows service/PowerShell arbitrario) explícitamente `PROHIBITED` — sólo visibilidad y manual hardening plan (`execution_status: NOT_EXECUTED`). Deepening de `agents/os-platform-analyst` (manifest real desde Foundation, mismo patrón que Security Fase 8) a contrato estructurado completo v2.0.0. Reemplaza el modelo platform-per-skill-id de Foundation (18 skills × 5 plataformas, sólo `os/linux/memory` materializado) por un modelo domain-per-skill-id (`os/<capability>`, platform-awareness dentro de cada manifest vía el campo `platforms:`), igual que `network/*`/`security/*`. Reutiliza (nunca duplica) los collectors OS certificados desde Fase 4 (`get_interfaces`, `get_routes`, `get_socket_summary`, `get_name_resolution`, `get_host_identity`), propiedad de `os-platform-analyst`. Ver `docs/PHASE_9_OS_PLATFORM_DIAGNOSTICS_HARDENING.md`, `docs/OS_READONLY_COLLECTOR_MODEL.md`. **Versión futura**: mismo criterio que Data Guard/Multitenant/RMAN/Security — la columna "latest" arriba deja de decir `SUPPORTED`, declara `COMPATIBILITY_VALIDATION_REQUIRED`. **PHASE 9 — EFFECTIVE PROCESS LIMITS & SYSTEMD/PAM SOURCE-OF-TRUTH HARDENING**: corrige un defecto real del build base — `os/systemd-limits` combinaba systemd y PAM/`limits.conf` mediante una regla universal de mínimo, estructuralmente incorrecta para un proceso lanzado por systemd sin `PAMName=` confirmado. `Effective process limits` Linux `SUPPORTED` (`get_process_effective_limits`, PID-scoped vía `/proc/<pid>/limits`), `systemd limit awareness` `SUPPORTED` (`launch_context`-aware, PAM ya no se asume aplicable sin evidencia), `PAM limit awareness` `SUPPORTED` (correlacionada sólo cuando el `launch_context` lo confirma), `cgroup pids/tasks awareness` `SUPPORTED` (Linux)/`PARTIALLY_SUPPORTED` (Solaris), `Solaris effective process limits` `PARTIALLY_SUPPORTED` (sin `/proc/<pid>/limits` equivalente certificado, nunca simula paridad Linux), `Windows effective process limits` `PARTIALLY_SUPPORTED` (sin API de job objects/resource limits certificada todavía), `Privilege escalation` `PROHIBITED` (nunca `sudo`/`su`/`runuser` — fallback `INSUFFICIENT_PRIVILEGES` + `manual_collection`). Ver `docs/PHASE_9_EFFECTIVE_PROCESS_LIMITS_SYSTEMD_PAM_HARDENING.md`. **PHASE 9 — PAM LIMITS APPLICABILITY & PROCESS CONSTRAINT SCOPE MICRO-HARDENING**: corrige dos defectos introducidos por el hardening anterior — `PAMName=` era tratado como prueba suficiente de PAM applicability sin verificar `pam_limits.so`, y `os/cgroups` seguía combinando `pids.max`/`nproc`/`TasksMax` mediante un mínimo universal. `PAM session awareness` `SUPPORTED` (`PAMName=` interpretado sólo como sesión PAM solicitada, nunca como `limits.conf` applicability), `pam_limits applicability` `SUPPORTED` (Linux, `get_pam_limits_applicability` confirma `pam_limits.so` en la pila PAM efectiva, estados `APPLICABLE`/`NOT_APPLICABLE`/`INSUFFICIENT_EVIDENCE`/`NOT_ASSESSED`)/`PARTIALLY_SUPPORTED` (Solaris/Windows), `PAM include-chain analysis` `SUPPORTED` (Linux, `max_depth`/`visited set` contra loops), `RLIMIT_NPROC` `SUPPORTED` (scope `USER`), `systemd TasksMax` `SUPPORTED` (scope `UNIT`), `cgroup pids.max` `SUPPORTED` (Linux, scope `CGROUP`, requiere membership confirmada)/`PARTIALLY_SUPPORTED` (Solaris), `binding-constraint analysis` `SUPPORTED` (Linux, calculado desde scope+membership+uso+headroom, nunca `min()` universal)/`PARTIALLY_SUPPORTED` (Solaris/Windows). Ver `docs/PHASE_9_PAM_LIMITS_PROCESS_CONSTRAINT_SCOPE_HARDENING.md`. **PHASE 9 — PAM LIMITS POLICY SOURCE & PID CONTROLLER IDENTITY MICRO-HARDENING**: corrige dos defectos introducidos por el micro-hardening anterior — `applicability == APPLICABLE` tratado como prueba de que `limits.conf`/`limits.d` eran la fuente efectiva sin parsear el `conf=` real de `pam_limits.so`, y `TasksMax`/`pids.max` del mismo cgroup contados dos veces como constraints independientes. `pam_limits default policy source` `SUPPORTED` (Linux, `mode: DEFAULT` con awareness de `limits.conf`/`limits.d`), `pam_limits custom conf source` `SUPPORTED` (Linux, `get_pam_limits_policy_source` detecta `conf=<path>`, tokenizado, nunca concurrente con la fuente default)/`PARTIALLY_SUPPORTED` (Solaris/Windows), `PAM module-argument parsing` `SUPPORTED` (Linux, `conf=`/`debug=`/`set_all=`/`utmp_early=`, múltiples invocaciones preservadas por separado), `TasksMax → pids.max mapping` `SUPPORTED` (Linux, `get_unit_cgroup_path` resuelve `unit_cgroup_path_token`, nunca adivinado por nombre de servicio), `PID-controller de-duplication` `SUPPORTED` (Linux, constraint canónico `PID_CONTROLLER` cuando `TasksMax` y `pids.max` resuelven al mismo `cgroup_path_token`, nunca contados dos veces), `cgroup hierarchy constraints` `SUPPORTED` (Linux, `relation UNIT/PARENT/CHILD/DELEGATED/OTHER`, nodos distintos nunca deduplicados por similitud numérica)/`PARTIALLY_SUPPORTED` (Solaris/Windows). Ver `docs/PHASE_9_PAM_POLICY_SOURCE_PID_CONTROLLER_IDENTITY_HARDENING.md`.
- **Capacity**: `SUPPORTED` desde Fase 10 (10g–23ai) — 28 skills `capacity/*` completamente materializados (`data-source-inventory`, `data-quality`, `normalization`, `cpu`, `memory`, `storage`, `oracle`, `asm`, `tablespace`, `os`, `windows`, `linux`, `sqlserver`, `vmware`, `horizontal`, `vertical`, `trend-analysis`, `forecasting`, `threshold-crossing`, `anomaly-awareness`, `seasonality-awareness`, `growth-rate`, `confidence`, `risk-classification`, `capacity-healthcheck`, `capacity-assessment`, `manual-capacity-plan`, `executive-summary`), reemplaza el modelo de 16 skill_ids `registered`-only de Foundation (1 activo previo, `capacity/forecast`, absorbido en `capacity/forecasting`). CPU/Memory/Storage `SUPPORTED` transversal Linux/Windows Server (vía `os-platform-analyst`, Fase 9, sin duplicar collectors — ver `docs/CAPACITY_OS_MODEL.md`). Oracle/ASM/Tablespace/FRA capacity `SUPPORTED` (vía `oracle-dba-analyst`/`oracle-asm-storage-analyst`/`oracle-backup-recovery-analyst`, Fase 2/4/7, por referencia — ver `docs/CAPACITY_ORACLE_MODEL.md`). Forecast 1/3/6 meses `SUPPORTED` (linear trend + confidence + reproducibilidad vía `forecast_contract_version`, nunca certeza — ver `docs/CAPACITY_FORECASTING_MODEL.md`). Threshold crossing `SUPPORTED` (ver `docs/CAPACITY_THRESHOLD_MODEL.md`). Horizontal/vertical analysis `SUPPORTED` (siempre advisory, `INSUFFICIENT_EVIDENCE` cuando corresponda, nunca forzado por umbral de utilización — ver `docs/CAPACITY_HORIZONTAL_VERTICAL_MODEL.md`). VMware capacity `PARTIALLY_SUPPORTED` (contract diseñado, `NOT_CERTIFIED` por defecto — sólo `MANUAL_IMPORT`, sin collector runtime en esta fase — ver `docs/CAPACITY_VMWARE_MODEL.md`). SQL Server capacity `PARTIALLY_SUPPORTED` (mismo criterio que VMware — ver `docs/CAPACITY_SQLSERVER_MODEL.md`). No depende exclusivamente de la versión Oracle — capacity es dominio transversal, la tabla lo repite igual en las columnas 10g–23ai por consistencia estructural con el resto de la matriz (igual que OS). Capacity mutation execution (resize VM, add CPU/memoria, extender filesystem/ASM/tablespace, alterar asignación VMware, cambiar parámetros DB/OS, modificar herramientas de monitoreo) explícitamente `PROHIBITED` — sólo forecast/análisis/`manual_action` con `execution_status: NOT_EXECUTED` (ver `docs/CAPACITY_MANUAL_ACTION_MODEL.md`). Un único agente `capacity-analyst` transversal, nunca dividido por recurso/tecnología. Deepening de `agents/capacity-analyst` (manifest real desde Foundation, mismo patrón que Security Fase 8/OS Fase 9) a contrato estructurado completo v2.0.0. **Versión futura**: mismo criterio que Data Guard/Multitenant/RMAN/Security/OS — la columna "latest" arriba deja de decir `SUPPORTED`, declara `COMPATIBILITY_VALIDATION_REQUIRED`. **PHASE 10 — FORECASTING EXECUTION & NUMERICAL VALIDATION HARDENING**: el forecasting/data-quality/threshold-crossing de la tabla dejan de ser sólo contrato — `capacity_engine/` (Python stdlib, `LOCAL_RUNTIME_TESTED`) implementa el pipeline normalización→calidad→segmentación→agregación→tendencia→forecast→umbral de forma ejecutable y determinista, verificado con 18 tests numéricos (`tests/test_capacity_engine_*.sh` + `test_forecast_linear_growth.sh`/`test_forecast_reproducible_same_input.sh` endurecidos) que comparan salidas del motor contra valores calculados independientemente, no contra cadenas de skills. `/healthcheck capacity`/`/assessment capacity` (orquestación de agente vivo) permanecen `CONTRACT_ONLY`/`NOT_RUNTIME_CERTIFIED` — dependen del MCP Gateway, fuera de alcance hasta esa fase; `capacity_engine.cli` es el adaptador local invocable que demuestra el recorrido real sin ese runtime. Ver `docs/PHASE_10_FORECASTING_EXECUTION_NUMERICAL_VALIDATION_HARDENING.md`, `docs/PHASE_10_CAPACITY_MANAGEMENT_FORECASTING.md`, `docs/CAPACITY_COMMON_METRIC_MODEL.md`, `docs/CAPACITY_DATA_SOURCE_MODEL.md`, `docs/CAPACITY_DATA_QUALITY_MODEL.md`, `docs/CAPACITY_REPORTING_MODEL.md`.
- **Incident Analysis & Root Cause Automation**: `SUPPORTED` desde Fase 11 (10g–23ai) — 34 skills `incident/*` completamente materializados (`intake`, `classification`, `severity-awareness`, `scope-identification`, `timeline`, `evidence-plan`, `evidence-correlation`, `symptom-clustering`, `hypothesis-generation`, `hypothesis-testing`, `contradiction-analysis`, `root-cause`, `contributing-factors`, `impact-analysis`, `blast-radius`, `recovery-status`, `recurrence-awareness`, `known-error-correlation`, `change-correlation`, `capacity-correlation`, más 9 skills de correlación cross-domain — `performance-correlation`, `rac-correlation`, `dataguard-correlation`, `asm-storage-correlation`, `network-correlation`, `os-correlation`, `security-correlation`, `rman-correlation`, `multitenant-correlation` —, y `manual-remediation-plan`, `post-incident-review`, `lessons-learned`, `incident-report`, `rca-report`), reemplaza el placeholder de 8 skill_ids de Foundation (1 activo previo, `incident/root-cause-analysis`, fusionado como base de `incident/root-cause` v2.0.0 — ver `docs/INCIDENT_ROOT_CAUSE_MODEL.md`). Un único agente `incident-root-cause-analyst` transversal (v2.0.0), nunca dividido por capability — mismo patrón que `capacity-analyst`/`os-platform-analyst`. No depende exclusivamente de la versión Oracle — el modelo de incidentes es transversal, la tabla lo repite igual en las columnas 10g–23ai; cada skill de correlación cross-domain hereda el boundary de versión/arquitectura del especialista que referencia (ej. `incident/rac-correlation` no aplica en targets standalone, igual que `rac`). **Root Cause Model** estricto (ver `docs/INCIDENT_ROOT_CAUSE_MODEL.md`, `docs/CONTRACTS.md#rca-model`): `CONFIRMED_ROOT_CAUSE` requiere 2 fuentes de evidencia independientes o prueba temporal inequívoca; `UNDETERMINED` es un estado terminal legítimo, nunca forzado bajo presión de "es un incidente". Correlación temporal **nunca** equivale a causalidad — `CHANGE_CORRELATED` != `CHANGE_CAUSED` es una distinción estructural (`incident/change-correlation`), igual que una acción de recuperación exitosa no prueba la causa raíz (`incident/recovery-status`). Blast radius/impacto nunca sobreestimados sin evidencia directa (`incident/blast-radius`, `incident/impact-analysis`). **Manual remediation ONLY** — incluso acciones de emergencia (restart, relocate, kill session, failover, extender filesystem) quedan siempre como `MANUAL AUTHORIZED ACTION` con `execution_status: NOT_EXECUTED`, sin excepción (`incident/manual-remediation-plan`) — mismo patrón que `change-advisor`/`capacity/manual-capacity-plan`/`os/manual-hardening-plan`/`rman/manual-recovery-plan`/`security/manual-remediation-plan`. Cross-domain correlation reutiliza (nunca duplica) `evidence_refs` de los 10 especialistas previos (`oracle-dba-analyst`, `oracle-performance-analyst`, `oracle-rac-analyst`, `oracle-asm-storage-analyst`, `oracle-dataguard-analyst`, `oracle-multitenant-analyst`, `oracle-backup-recovery-analyst`, `oracle-network-analyst`, `oracle-security-analyst`, `os-platform-analyst`, `capacity-analyst`). Un forecast de capacidad futuro **nunca** se usa como prueba de causa de un incidente ya ocurrido sin evidencia histórica/actual directa (`incident/capacity-correlation`). RMAN execution explícitamente `PROHIBITED` incluso durante investigación de incidente (`incident/rman-correlation` nunca ejecuta RMAN). **Versión futura**: mismo criterio que Data Guard/Multitenant/RMAN/Security/OS/Capacity — la columna "latest" arriba deja de decir `SUPPORTED`, declara `COMPATIBILITY_VALIDATION_REQUIRED`. **PHASE 11 — RCA EXECUTION & EVIDENCE VALIDATION HARDENING**: `incident/root-cause`, `incident/timeline`, `incident/hypothesis-generation`, `incident/hypothesis-testing` y `incident/contradiction-analysis` de la tabla dejan de ser sólo contrato — `rca_engine/` (Python stdlib, `LOCAL_RUNTIME_TESTED`) implementa el pipeline intake→normalización de timeline (UTC, clock skew)→motor de hipótesis basado en reglas versionadas/allowlisted→gate de confirmación (2 fuentes independientes o prueba temporal inequívoca, contradicciones críticas bloquean `CONFIRMED`, referencias de evidencia rotas bloquean `CONFIRMED`)→JSON/Markdown/evidence manifest de forma ejecutable y determinista, verificado con 19 tests funcionales (`tests/test_rca_*.sh`) que ejecutan el CLI real contra fixtures sintéticos y un control de mutation testing. `/diagnose incident`/`/rca`/`/healthcheck incident` (orquestación de agente vivo) permanecen `CONTRACT_ONLY`/`NOT_RUNTIME_CERTIFIED` — dependen del MCP Gateway, fuera de alcance hasta esa fase; `rca_engine.cli` es el adaptador local invocable que demuestra el recorrido real sin ese runtime. Ver `docs/PHASE_11_RCA_EXECUTION_EVIDENCE_VALIDATION_HARDENING.md`, `docs/PHASE_11_INCIDENT_ANALYSIS_ROOT_CAUSE_AUTOMATION.md`, `docs/INCIDENT_INTAKE_MODEL.md`, `docs/INCIDENT_EVIDENCE_MODEL.md`, `docs/INCIDENT_TIMELINE_MODEL.md`, `docs/INCIDENT_HYPOTHESIS_MODEL.md`, `docs/INCIDENT_CAUSALITY_MODEL.md`, `docs/INCIDENT_ROOT_CAUSE_MODEL.md`, `docs/INCIDENT_IMPACT_MODEL.md`, `docs/INCIDENT_PLAYBOOK_MODEL.md`, `docs/INCIDENT_MANUAL_REMEDIATION_MODEL.md`, `docs/INCIDENT_POSTMORTEM_MODEL.md`, `docs/INCIDENT_READONLY_SECURITY_MODEL.md`.
- **Documentation**: un skill representativo materializado; el resto `registered` (ver `skills/REGISTRY.md`). No depende de la versión Oracle — la tabla lo repite igual en las 8 columnas por consistencia estructural con el resto de la matriz.
- **Change Advisory, Documentation & Knowledge Lifecycle** (Fase 12): 6 skills `change/*` nuevos, 5 `documentation/*` (nuevos o materializados) y el dominio `knowledge/*` (7) son ejecutables vía `change_documentation_knowledge/` (`LOCAL_RUNTIME_TESTED`, CLI `advise`/`document`/`kb-*`) y no añaden una fila de dominio: son procesos sobre artefactos ya saneados, independientes de la versión Oracle del target. La asesoría de cambio es sólo texto para ejecución humana (`NOT_EXECUTED_BY_ESTACK`); los gates capability/license/privilege/change_window quedan `UNKNOWN` salvo declaración explícita y nunca se infieren de la versión (ni 19c como cobertura universal, ni licencias AWR/ASH/ADDM/Active Data Guard por defecto). Aprobación/publicación de conocimiento sólo con registro de autorización humano externo (identidad no verificable por el e-stack). Ver `docs/PHASE_12_CHANGE_ADVISORY_DOCUMENTATION_KNOWLEDGE_LIFECYCLE.md`.
- **MCP Diagnostic Gateway** (Fase 13): `PARTIAL` en todas las versiones **a propósito** — el servidor MCP local (`mcp_gateway/`, stdio) está verificado sólo con el adaptador `FIXTURE` (`VERIFIED_FIXTURE`, datos sintéticos). Los adaptadores reales están `DISABLED`/`CONTRACT_ONLY` y `NOT_INTEGRATION_TESTED`: no hay entorno de laboratorio aprobado y ninguna conexión real es alcanzable desde una tool, un flag, una variable de entorno o el archivo de destinos. La cobertura por versión/rol/licencia se evalúa por target en cada llamada (nunca se asume). Ver `docs/PHASE_13_MCP_DIAGNOSTIC_GATEWAY.md`.
- **/change**: el proceso de gobierno está completo y probado (`tests/test_change_governance_flow.sh`) independientemente de la versión del target — es un proceso sobre el propio e-stack, no sobre el ambiente Oracle.

## Actualización

Toda incorporación de una nueva versión Oracle o ampliación de cobertura de un dominio entra vía `/change compatibility` o `/change skill`/`/change query`, actualizando **ambos** archivos (`config/capability-matrix.yaml` y este Markdown) en el mismo cambio — ver `EVOLUTION.md#13-change-compatibility`.
