# Fixture: Oracle 19c RAC — ASM disk group con USABLE_FILE_MB crítico pese a FREE_MB aparentemente razonable (# 54)
# Usada por: asm/capacity — prueba que el finding usa el conjunto completo, no FREE_MB aislado
fixture_id: FX-19c-asm-low-usable-capacity

target_profile:
  schema_version: "2.1.0"
  target_id: "fixture-19c-asm-low-usable"
  database: {name: "ORCL19CLU", unique_name: "ORCL19CLU", dbid: "1922223330"}
  oracle_version: {major: 19, minor: 0, release: 0, ru: "19.21", raw: "19.21.0.0.0"}
  architecture: {cluster_mode: rac, multitenant_mode: cdb, storage_mode: asm}
  database_role: primary
  open_mode: "READ WRITE"
  log_mode: archivelog
  force_logging: true
  instance: {name: "orcl19clu1", number: 1, host: "HOST_TOKEN_1001"}
  cluster: {detected: true, instance_count: 2}
  container: {type: cdb_root, con_id: 1, con_name: "CDB$ROOT"}
  platform: {database_platform: "Linux x86 64-bit"}
  capabilities: {rac: SUPPORTED, asm: SUPPORTED, multitenant: SUPPORTED, dataguard: UNSUPPORTED, awr: LICENSE_RESTRICTED, ash: LICENSE_RESTRICTED}
  rac: {enabled: true, node_count: 2, instances: ["INSTANCE_TOKEN_001", "INSTANCE_TOKEN_002"]}
  gi: {version: "19.21.0.0.0", home: "PATH_TOKEN_010", cluster_name: "CLUSTER_TOKEN_010"}
  asm: {enabled: true, instance_count: 2}
  network: {scan_name: "SCAN_TOKEN_010", scan_ips: ["IP_TOKEN"], listener_ports: [1521]}
  discovery: {timestamp: "2026-01-19T09:00:00Z", evidence_refs: [EVD-FX-1001], confidence: FACT}

compatibility_schema:
  available_views: [V$INSTANCE, GV$INSTANCE, V$DATABASE, V$ACTIVE_INSTANCES, GV$SERVICES,
    GV$ACTIVE_SERVICES, GV$SESSION, GV$CLUSTER_INTERCONNECTS, GV$GES_STATISTICS, GV$GCS_STATISTICS,
    GV$ASM_INSTANCE, V$ASM_DISKGROUP_STAT, V$ASM_DISK, GV$ASM_OPERATION]
  available_columns:
    "V$INSTANCE.version_full": true
    "V$DATABASE.cdb": true
    "V$ACTIVE_INSTANCES.con_id": true
    "V$INSTANCE.instance_role": true

mock_evidence:
  - query_id: Q-ASM-TOPOLOGY-001
    result:
      - {inst_id: 1, instance_name: "+ASM1", status: "STARTED", diskgroup: "DATA", state: "MOUNTED", type: "HIGH", total_mb: 300000, free_mb: 90000, usable_file_mb: 8000, required_mirror_free_mb: 82000}

expected_findings:
  - category: capacity
    severity: CRITICAL
    confidence: FACT
    reason: "free_mb=90000 parece razonable (30%) pero redundancy=HIGH (triple mirror) + required_mirror_free_mb=82000 dejan usable_file_mb=8000 (2.7% real) — el finding se basa en USABLE_FILE_MB, no en FREE_MB aislado (# 24, # 54)"
