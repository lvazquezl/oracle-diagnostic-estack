# Fixture: Oracle 23ai Standalone CDB — performance moderno, sin anomalías
# Usada por: version-awareness cruzada de Fase 3 (23ai known_supported)
fixture_id: FX-23ai-modern-performance

target_profile:
  schema_version: "2.0.0"
  target_id: "fixture-23ai-perf"
  database:
    name: "CDB23AIPERF"
    unique_name: "CDB23AIPERF"
    dbid: "9996567890"
  oracle_version: {major: 23, minor: 4, release: 0, ru: null, raw: "23.4.0.24.05"}
  architecture: {cluster_mode: standalone, multitenant_mode: cdb, storage_mode: filesystem}
  database_role: primary
  open_mode: "READ WRITE"
  log_mode: archivelog
  force_logging: false
  instance: {name: "cdb23aiperf", number: 1, host: "HOST_TOKEN_030"}
  cluster: {detected: false, instance_count: 1}
  container: {type: cdb_root, con_id: 1, con_name: "CDB$ROOT"}
  platform: {database_platform: "Linux x86 64-bit"}
  capabilities:
    rac: UNSUPPORTED
    asm: UNSUPPORTED
    multitenant: SUPPORTED
    dataguard: UNSUPPORTED
    awr: SUPPORTED
    ash: SUPPORTED
  pdbs: ["APPPDB"]
  discovery: {timestamp: "2026-01-16T09:50:00Z", evidence_refs: [EVD-FX-030], confidence: FACT}

license_confirmed: {diagnostics_pack: true, tuning_pack: false}

compatibility_schema:
  available_views: [V$INSTANCE, GV$INSTANCE, V$DATABASE, V$ACTIVE_INSTANCES, V$PARAMETER,
    GV$PARAMETER, V$SPPARAMETER, V$SESSION, GV$SESSION, V$PROCESS, V$RESOURCE_LIMIT,
    DBA_HIST_SNAPSHOT, DBA_HIST_SYSTEM_EVENT, DBA_HIST_SYS_TIME_MODEL, DBA_HIST_SQLSTAT,
    V$ACTIVE_SESSION_HISTORY, DBA_HIST_ACTIVE_SESS_HISTORY, V$SYS_TIME_MODEL, V$SQLSTATS,
    V$SQL_PLAN, V$SGA, V$SGAINFO, V$SGASTAT, V$PGASTAT, V$SYSSTAT, V$LIBRARYCACHE, V$ROWCACHE,
    V$SYSTEM_EVENT, V$FILESTAT, V$DATAFILE, V$SORT_USAGE, V$LOCK, V$PX_SESSION]
  available_columns:
    "V$INSTANCE.version_full": true      # 18.0+
    "V$INSTANCE.instance_role": true     # 11.0+
    "V$DATABASE.cdb": true                # 12.1+
    "V$ACTIVE_INSTANCES.con_id": true     # 12.1+
    "V$SESSION.con_id": true              # 12.1+
    "DBA_HIST_SYSTEM_EVENT.time_waited_micro_fg": true    # 11.0+

mock_evidence:
  - query_id: Q-PERF-DBTIME-001
    result:
      - {instance_number: 1, db_time_sec: 600.0, db_cpu_sec: 400.0, elapsed_sec: 3600.0}
  - query_id: Q-PERF-WAIT-AWR-001
    result:
      - {instance_number: 1, event_name: "db file sequential read", wait_class: "User I/O", total_wait_time_sec: 120.0}
  - query_id: Q-PERF-SGA-001
    result:
      sga_total_bytes: 17179869184
      sga_max_bytes: 17179869184
      sga_free_bytes: 3221225472
      shared_pool_bytes: 4294967296
      buffer_cache_bytes: 10737418240
      large_pool_bytes: 536870912
      java_pool_bytes: 268435456

expected_findings:
  - category: awr-analysis
    severity: INFO
    confidence: FACT
    reason: "db_cpu_pct(db_time)=67%, sin waits dominantes anormales, SGA con margen saludable — workload dentro de lo esperado; 23ai clasifica known_supported hoy (docs/QUERY_VARIANTS.md#future-proof-version-policy)"
