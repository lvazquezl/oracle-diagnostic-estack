# Fixture: Oracle 19c — FSFO habilitado con observer conectado (usa show-fsfo.txt)
# Usada por: dataguard/fsfo, dataguard/observer, dataguard/failover-readiness
fixture_id: FX-19c-fsfo-enabled

target_profile:
  schema_version: "2.2.0"
  target_id: "fixture-19c-dg-fsfo"
  database: {name: "ORCL19FS", unique_name: "orcl19fs_pri", dbid: "1923223331"}
  oracle_version: {major: 19, minor: 0, release: 0, ru: "19.21", raw: "19.21.0.0.0"}
  architecture: {cluster_mode: standalone, multitenant_mode: cdb, storage_mode: asm}
  database_role: primary
  open_mode: "READ WRITE"
  log_mode: archivelog
  force_logging: true
  instance: {name: "orcl19fs", number: 1, host: "HOST_TOKEN_PRI03"}
  cluster: {detected: false, instance_count: 1}
  container: {type: cdb_root, con_id: 1, con_name: "CDB$ROOT"}
  platform: {database_platform: "Linux x86 64-bit"}
  capabilities: {rac: UNSUPPORTED, asm: SUPPORTED, multitenant: SUPPORTED, dataguard: SUPPORTED, awr: LICENSE_RESTRICTED, ash: LICENSE_RESTRICTED}
  dataguard: {enabled: true, role: primary, db_unique_name: "DB_UNIQUE_NAME_TOKEN_001", protection_mode: MAXIMUM_AVAILABILITY, broker_enabled: true, fsfo_enabled: true, primary: "DB_UNIQUE_NAME_TOKEN_001", standbys: ["DB_UNIQUE_NAME_TOKEN_004"]}
  discovery: {timestamp: "2026-02-13T09:00:00Z", evidence_refs: [EVD-FX-3201], confidence: FACT}

compatibility_schema:
  available_views: [V$DATABASE, V$DATAGUARD_STATS, V$ARCHIVE_DEST, V$ARCHIVE_DEST_STATUS,
    V$ARCHIVED_LOG, V$ARCHIVE_GAP, V$MANAGED_STANDBY, GV$MANAGED_STANDBY, V$STANDBY_LOG, V$LOG]
  available_columns:
    "V$INSTANCE.version_full": true
    "V$DATABASE.cdb": true
    "V$ACTIVE_INSTANCES.con_id": true
    "V$INSTANCE.instance_role": true

collector_fixture_files:
  get_fsfo_status: tests/fixtures/broker/show-fsfo.txt

expected_findings:
  - category: fsfo
    severity: INFO
    confidence: FACT
    reason: "FSFO enabled, protection_mode=MAXIMUM AVAILABILITY (consistente), observer_status=CONNECTED (Registered: 1) → sin riesgo de FSFO inoperante"
