# Fixture: Oracle 19c Standalone CDB — DB CPU dominante, top SQL concentrado
# Usada por: performance/db-cpu, performance/db-time, performance/awr-analysis, performance/hard-parse
fixture_id: FX-19c-high-cpu

target_profile:
  schema_version: "2.0.0"
  target_id: "fixture-19c-perf-highcpu"
  database:
    name: "CDB19CHIGHCPU"
    unique_name: "CDB19CHIGHCPU"
    dbid: "9934567890"
  oracle_version: {major: 19, minor: 0, release: 0, ru: "19.21", raw: "19.21.0.0.0"}
  architecture: {cluster_mode: standalone, multitenant_mode: cdb, storage_mode: filesystem}
  database_role: primary
  open_mode: "READ WRITE"
  log_mode: archivelog
  force_logging: true
  instance: {name: "cdb19chighcpu", number: 1, host: "HOST_TOKEN_027"}
  cluster: {detected: false, instance_count: 1}
  container: {type: cdb_root, con_id: 1, con_name: "CDB$ROOT"}
  platform: {database_platform: "Linux x86 64-bit"}
  capabilities:
    rac: UNSUPPORTED
    asm: UNSUPPORTED
    multitenant: SUPPORTED
    dataguard: UNSUPPORTED
    awr: SUPPORTED
    ash: SUPPORTED
  pdbs: ["APPPDB"]
  discovery: {timestamp: "2026-01-16T09:35:00Z", evidence_refs: [EVD-FX-027], confidence: FACT}

license_confirmed: {diagnostics_pack: true, tuning_pack: false}

compatibility_schema:
  available_views: [V$INSTANCE, GV$INSTANCE, V$DATABASE, V$ACTIVE_INSTANCES, V$PARAMETER,
    GV$PARAMETER, V$SPPARAMETER, V$SESSION, GV$SESSION, V$PROCESS, V$RESOURCE_LIMIT,
    DBA_HIST_SNAPSHOT, DBA_HIST_SYSTEM_EVENT, DBA_HIST_SYS_TIME_MODEL, DBA_HIST_SQLSTAT,
    V$ACTIVE_SESSION_HISTORY, DBA_HIST_ACTIVE_SESS_HISTORY, V$SYS_TIME_MODEL, V$SQLSTATS,
    V$SQL_PLAN, V$SYSSTAT]
  available_columns:
    "V$INSTANCE.version_full": true      # 18.0+
    "V$INSTANCE.instance_role": true     # 11.0+
    "V$DATABASE.cdb": true                # 12.1+
    "V$ACTIVE_INSTANCES.con_id": true     # 12.1+
    "V$SESSION.con_id": true              # 12.1+
    "DBA_HIST_SYSTEM_EVENT.time_waited_micro_fg": true    # 11.0+

mock_evidence:
  - query_id: Q-PERF-DBTIME-001
    result:
      - {instance_number: 1, db_time_sec: 3400.0, db_cpu_sec: 3100.0, elapsed_sec: 3600.0}
  - query_id: Q-PERF-WAIT-AWR-001
    result:
      - {instance_number: 1, event_name: "db file sequential read", wait_class: "User I/O", total_wait_time_sec: 45.0}
  - query_id: Q-PERF-TOPSQL-001
    result:
      - {sql_id: "ijk234lmn08", plan_hash_value: 4444444444, executions: 200000, elapsed_sec: 2800.0, cpu_sec: 2650.0, buffer_gets: 12000000, disk_reads: 500, rows_processed: 200000}
  - query_id: Q-PERF-HARDPARSE-001
    result:
      parse_count_total: 800000
      parse_count_hard: 350000
      session_cursor_cache_hits: 90000
      execute_count: 900000

expected_findings:
  - category: db-cpu
    severity: HIGH
    confidence: HYPOTHESIS
    reason: "db_cpu_pct(db_time)=91%, top SQL cpu_sec=2650/3100=85% concentrado en un sql_id, hard_parse_ratio=350000/800000=44% desproporcionado → HYPOTHESIS: CPU-bound agravado por hard parsing excesivo (literales sin bind variables, candidato de investigación manual)"
