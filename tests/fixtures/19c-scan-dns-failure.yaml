# Fixture: Oracle 19c RAC — SCAN configurado pero DNS no resuelve (SERVFAIL)
# Usada por: network/scan-resolution, network/connection-path (clasificación NAME_RESOLUTION)
fixture_id: FX-19c-scan-dns-failure

target_profile:
  schema_version: "2.1.0"
  target_id: "fixture-19c-scan-dns-failure"
  database: {name: "ORCL19CDF", unique_name: "ORCL19CDF", dbid: "1918223330"}
  oracle_version: {major: 19, minor: 0, release: 0, ru: "19.21", raw: "19.21.0.0.0"}
  architecture: {cluster_mode: rac, multitenant_mode: cdb, storage_mode: asm}
  database_role: primary
  open_mode: "READ WRITE"
  log_mode: archivelog
  force_logging: true
  instance: {name: "orcl19cdf1", number: 1, host: "HOST_TOKEN_601"}
  cluster: {detected: true, instance_count: 2}
  container: {type: cdb_root, con_id: 1, con_name: "CDB$ROOT"}
  platform: {database_platform: "Linux x86 64-bit"}
  capabilities: {rac: SUPPORTED, asm: SUPPORTED, multitenant: SUPPORTED, dataguard: UNSUPPORTED, awr: LICENSE_RESTRICTED, ash: LICENSE_RESTRICTED}
  rac: {enabled: true, node_count: 2, instances: ["INSTANCE_TOKEN_001", "INSTANCE_TOKEN_002"]}
  gi: {version: "19.21.0.0.0", home: "PATH_TOKEN_006", cluster_name: "CLUSTER_TOKEN_006"}
  asm: {enabled: true, instance_count: 2}
  network: {scan_name: "SCAN_TOKEN_006", scan_ips: null, listener_ports: [1521]}
  discovery: {timestamp: "2026-01-15T09:00:00Z", evidence_refs: [EVD-FX-601], confidence: OBSERVATION}

compatibility_schema:
  available_views: [V$INSTANCE, GV$INSTANCE, V$DATABASE, V$ACTIVE_INSTANCES, GV$SERVICES,
    GV$ACTIVE_SERVICES, GV$SESSION, GV$CLUSTER_INTERCONNECTS, GV$GES_STATISTICS, GV$GCS_STATISTICS,
    GV$ASM_INSTANCE, V$ASM_DISKGROUP_STAT, V$ASM_DISK, GV$ASM_OPERATION]
  available_columns:
    "V$INSTANCE.version_full": true
    "V$DATABASE.cdb": true
    "V$ACTIVE_INSTANCES.con_id": true
    "V$INSTANCE.instance_role": true

collector_output:
  get_name_resolution: {resolution_status: SERVFAIL, resolved_ip_count: 0}

expected_findings:
  - category: scan-resolution
    severity: HIGH
    confidence: HYPOTHESIS
    reason: "resolution_status=SERVFAIL → clasificación NAME_RESOLUTION (network/connection-path), recomendación manual al equipo de red/DNS, nunca 'firewall' como primera hipótesis"
