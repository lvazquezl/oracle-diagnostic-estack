# Fixture: Oracle 19c RAC CDB (3 instancias) — AWR multi-instancia (Diagnostics Pack confirmado)
# Usada por: multi-instance awareness (agents/oracle-performance-analyst/AGENT.md#multi-instance-awareness)
fixture_id: FX-19c-rac-multi-instance

target_profile:
  schema_version: "2.0.0"
  target_id: "fixture-19c-perf-rac"
  database:
    name: "CDB19CRACPERF"
    unique_name: "CDB19CRACPERF"
    dbid: "9634567890"
  oracle_version: {major: 19, minor: 0, release: 0, ru: "19.21", raw: "19.21.0.0.0"}
  architecture: {cluster_mode: rac, multitenant_mode: cdb, storage_mode: asm}
  database_role: primary
  open_mode: "READ WRITE"
  log_mode: archivelog
  force_logging: true
  instance: {name: "cdb19cracperf1", number: 1, host: "HOST_TOKEN_024A"}
  cluster: {detected: true, instance_count: 3}
  container: {type: cdb_root, con_id: 1, con_name: "CDB$ROOT"}
  platform: {database_platform: "Linux x86 64-bit"}
  capabilities:
    rac: SUPPORTED
    asm: SUPPORTED
    multitenant: SUPPORTED
    dataguard: UNSUPPORTED
    awr: SUPPORTED
    ash: SUPPORTED
  pdbs: ["SALESPDB"]
  discovery: {timestamp: "2026-01-16T09:20:00Z", evidence_refs: [EVD-FX-024], confidence: FACT}

license_confirmed: {diagnostics_pack: true, tuning_pack: false}

compatibility_schema:
  available_views: [V$INSTANCE, GV$INSTANCE, V$DATABASE, V$ACTIVE_INSTANCES, V$PARAMETER,
    GV$PARAMETER, V$SPPARAMETER, V$SESSION, GV$SESSION, V$PROCESS, V$RESOURCE_LIMIT,
    DBA_HIST_SNAPSHOT, DBA_HIST_SYSTEM_EVENT, DBA_HIST_SYS_TIME_MODEL, DBA_HIST_SQLSTAT,
    DBA_HIST_SQL_PLAN, V$ACTIVE_SESSION_HISTORY, DBA_HIST_ACTIVE_SESS_HISTORY, V$SYS_TIME_MODEL,
    V$SQLSTATS, V$SQL_PLAN, V$SGA, V$SGAINFO, V$SGASTAT, V$PGASTAT, V$SYSSTAT, V$LIBRARYCACHE,
    V$ROWCACHE, V$SYSTEM_EVENT, V$FILESTAT, V$DATAFILE, V$SORT_USAGE, V$LOCK, V$PX_SESSION]
  available_columns:
    "V$INSTANCE.version_full": true      # 18.0+
    "V$INSTANCE.instance_role": true     # 11.0+
    "V$DATABASE.cdb": true                # 12.1+
    "V$ACTIVE_INSTANCES.con_id": true     # 12.1+
    "V$SESSION.con_id": true              # 12.1+
    "DBA_HIST_SYSTEM_EVENT.time_waited_micro_fg": true    # 11.0+

mock_evidence:
  - query_id: Q-PERF-DBTIME-001
    result:
      - {instance_number: 1, db_time_sec: 1200.0, db_cpu_sec: 700.0, elapsed_sec: 3600.0}
      - {instance_number: 2, db_time_sec: 900.0, db_cpu_sec: 500.0, elapsed_sec: 3600.0}
      - {instance_number: 3, db_time_sec: 300.0, db_cpu_sec: 100.0, elapsed_sec: 3600.0}
  - query_id: Q-PERF-WAIT-AWR-001
    result:
      - {instance_number: 1, event_name: "gc cr block busy", wait_class: "Cluster", total_wait_time_sec: 220.0}
      - {instance_number: 2, event_name: "gc buffer busy acquire", wait_class: "Cluster", total_wait_time_sec: 180.0}

expected_findings:
  - category: multi-instance-awareness
    severity: MEDIUM
    confidence: OBSERVATION
    reason: "DB Time desbalanceado entre instancias (1200/900/300 sec) — nunca agregado ingenuamente; wait dominante Cluster (gc *) → next_skill_or_agent: oracle-rac-analyst, no interpretado internamente"
