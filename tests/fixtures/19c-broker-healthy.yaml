# Fixture: Oracle 19c — Broker configuration sana (usa tests/fixtures/broker/show-configuration.txt + show-database.txt)
# Usada por: dataguard/broker
fixture_id: FX-19c-broker-healthy

target_profile:
  schema_version: "2.2.0"
  target_id: "fixture-19c-dg-brokerok"
  database: {name: "ORCL19BH", unique_name: "orcl19bh_sby", dbid: "1921223331"}
  oracle_version: {major: 19, minor: 0, release: 0, ru: "19.21", raw: "19.21.0.0.0"}
  architecture: {cluster_mode: standalone, multitenant_mode: cdb, storage_mode: asm}
  database_role: physical_standby
  open_mode: "READ ONLY WITH APPLY"
  log_mode: archivelog
  force_logging: true
  instance: {name: "orcl19bh", number: 1, host: "HOST_TOKEN_SBY09"}
  cluster: {detected: false, instance_count: 1}
  container: {type: cdb_root, con_id: 1, con_name: "CDB$ROOT"}
  platform: {database_platform: "Linux x86 64-bit"}
  capabilities: {rac: UNSUPPORTED, asm: SUPPORTED, multitenant: SUPPORTED, dataguard: SUPPORTED, awr: LICENSE_RESTRICTED, ash: LICENSE_RESTRICTED}
  dataguard: {enabled: true, role: physical_standby, db_unique_name: "DB_UNIQUE_NAME_TOKEN_001", protection_mode: MAXIMUM_AVAILABILITY, broker_enabled: true, fsfo_enabled: true, primary: "DB_UNIQUE_NAME_TOKEN_002", standbys: null}
  discovery: {timestamp: "2026-02-11T09:00:00Z", evidence_refs: [EVD-FX-3001], confidence: FACT}

compatibility_schema:
  available_views: [V$DATABASE, V$DATAGUARD_STATS, V$ARCHIVE_DEST, V$ARCHIVE_DEST_STATUS,
    V$ARCHIVED_LOG, V$ARCHIVE_GAP, V$MANAGED_STANDBY, GV$MANAGED_STANDBY, V$STANDBY_LOG, V$LOG]
  available_columns:
    "V$INSTANCE.version_full": true
    "V$DATABASE.cdb": true
    "V$ACTIVE_INSTANCES.con_id": true
    "V$INSTANCE.instance_role": true

collector_fixture_files:
  get_dataguard_configuration: tests/fixtures/broker/show-configuration.txt
  get_dataguard_database_status: tests/fixtures/broker/show-database.txt

expected_findings:
  - category: broker
    severity: INFO
    confidence: FACT
    reason: "Configuration Status=SUCCESS, ambos miembros con Intended State consistente, sin warnings/errores"
