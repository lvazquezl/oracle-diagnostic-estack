# Fixture: Oracle 12c CDB — AWR (Diagnostics Pack confirmado)
# Usada por: test_awr_parsing.sh, test_awr_db_time.sh, test_awr_wait_events.sh
fixture_id: FX-12c-awr

target_profile:
  schema_version: "2.0.0"
  target_id: "fixture-12c-awr"
  database:
    name: "CDB12CPERF"
    unique_name: "CDB12CPERF"
    dbid: "9434567890"
  oracle_version: {major: 12, minor: 1, release: 0, ru: null, raw: "12.1.0.2.0"}
  architecture: {cluster_mode: standalone, multitenant_mode: cdb, storage_mode: filesystem}
  database_role: primary
  open_mode: "READ WRITE"
  log_mode: archivelog
  force_logging: true
  instance: {name: "cdb12cperf", number: 1, host: "HOST_TOKEN_022"}
  cluster: {detected: false, instance_count: 1}
  container: {type: cdb_root, con_id: 1, con_name: "CDB$ROOT"}
  platform: {database_platform: "Linux x86 64-bit"}
  capabilities:
    rac: UNSUPPORTED
    asm: UNSUPPORTED
    multitenant: SUPPORTED
    dataguard: UNSUPPORTED
    awr: SUPPORTED
    ash: SUPPORTED
  pdbs: ["APPPDB"]
  discovery: {timestamp: "2026-01-16T09:10:00Z", evidence_refs: [EVD-FX-022], confidence: FACT}

license_confirmed: {diagnostics_pack: true, tuning_pack: false}

compatibility_schema:
  available_views: [V$INSTANCE, GV$INSTANCE, V$DATABASE, V$ACTIVE_INSTANCES, V$PARAMETER,
    GV$PARAMETER, V$SPPARAMETER, V$SESSION, GV$SESSION, V$PROCESS, V$RESOURCE_LIMIT,
    DBA_HIST_SNAPSHOT, DBA_HIST_SYSTEM_EVENT, DBA_HIST_SYS_TIME_MODEL, DBA_HIST_SQLSTAT,
    DBA_HIST_SQL_PLAN, V$ACTIVE_SESSION_HISTORY, DBA_HIST_ACTIVE_SESS_HISTORY, V$SYS_TIME_MODEL,
    V$SQLSTATS, V$SQL_PLAN, V$SGA, V$SGAINFO, V$SGASTAT, V$PGASTAT, V$SYSSTAT, V$LIBRARYCACHE,
    V$ROWCACHE, V$SYSTEM_EVENT, V$FILESTAT, V$DATAFILE, V$SORT_USAGE, V$LOCK, V$PX_SESSION]
  available_columns:
    "V$INSTANCE.version_full": false     # 18.0+
    "V$INSTANCE.instance_role": true     # 11.0+
    "V$DATABASE.cdb": true                # 12.1+
    "V$ACTIVE_INSTANCES.con_id": true     # 12.1+
    "V$SESSION.con_id": true              # 12.1+
    "DBA_HIST_SYSTEM_EVENT.time_waited_micro_fg": true    # 11.0+

mock_evidence:
  - query_id: Q-PERF-DBTIME-001
    result:
      - {instance_number: 1, db_time_sec: 1800.0, db_cpu_sec: 900.0, elapsed_sec: 3600.0}
  - query_id: Q-PERF-WAIT-AWR-001
    result:
      - {instance_number: 1, event_name: "db file sequential read", wait_class: "User I/O", total_wait_time_sec: 540.0}
      - {instance_number: 1, event_name: "log file sync", wait_class: "Commit", total_wait_time_sec: 180.0}
  - query_id: Q-PERF-TOPSQL-001
    result:
      - {sql_id: "abc123xyz01", plan_hash_value: 1111111111, executions: 500, elapsed_sec: 420.0, cpu_sec: 300.0, buffer_gets: 900000, disk_reads: 12000, rows_processed: 50000}

expected_findings:
  - category: awr-analysis
    severity: LOW
    confidence: FACT
    reason: "db_cpu_pct(db_time)=50%, top wait User I/O consistente con workload esperado — sin anomalía"
