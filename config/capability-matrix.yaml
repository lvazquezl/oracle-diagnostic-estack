# Fuente estructurada de verdad de la Capability Matrix. docs/CAPABILITY_MATRIX.md debe mantenerse
# consistente con este archivo (ver tests/test_capability_matrix_registry_consistency.sh).
#
# status values: PLANNED | FOUNDATION_ONLY | PARTIAL | SUPPORTED | UNSUPPORTED | LICENSE_DEPENDENT
#   PLANNED           — la feature Oracle existe en esa versión, pero el e-stack aún no tiene
#                        contrato/skill/query ni siquiera registrado para ella.
#   FOUNDATION_ONLY   — el dominio tiene agente/contrato/skills REGISTERED, pero ningún skill
#                        materializado (status: active) todavía cubre esta versión.
#   PARTIAL           — al menos un skill materializado cubre esta versión, pero no el dominio completo.
#   SUPPORTED         — cobertura completa certificada (no ocurre aún en Fase 1/Hardening).
#   UNSUPPORTED       — la feature Oracle NO EXISTE en esa versión/arquitectura (no es límite del e-stack).
#   LICENSE_DEPENDENT — depende de licenciamiento adicional (Diagnostics/Tuning Pack, etc.) independientemente
#                        de la cobertura del e-stack; ver policies/licensing-awareness-policy.md.
#
# "latest" trackea la versión mayor soportada más reciente (hoy: 23ai) hasta que se agregue una nueva
# vía /change compatibility.

schema_version: 1
generated_from: "Foundation Hardening pass"
domains:
  - id: oracle-core
    display_name: "Oracle Core"
    license_dependent: false
    versions: {10g: SUPPORTED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED, latest: SUPPORTED}
    notes: "Fase 2: las 18 skills oracle/* completamente materializadas (SKILL.md + manifest.yaml), ~26 queries Q-ORA-*/Q-DISC-*/Q-DBA-* certificadas. Compatibility Hardening (config/query-compatibility-matrix.yaml) validó que cada versión declarada tiene una variante SQL ejecutable real (Query Variant Resolver), no sólo metadata — ver docs/QUERY_VARIANTS.md y docs/PHASE_2_COMPATIBILITY_HARDENING.md. diagnostics (ADR) requiere 11g+; en 10g degrada a UNSUPPORTED para ese sub-componente específico. jobs/DBA_JOBS (legacy DBMS_JOB) es un gap documentado y PLANNED en todas las versiones — el sub-componente DBMS_SCHEDULER (Q-ORA-JOBS-SUMMARY-001) sí está SUPPORTED 10g–23ai; ver skills/oracle/jobs/SKILL.md. El resto de Oracle Core aplica igual en todas las versiones declaradas."

  - id: performance
    display_name: "Performance"
    license_dependent: false
    versions: {10g: SUPPORTED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED, latest: SUPPORTED}
    notes: "Fase 3: 31 skills performance/* completamente materializadas (SKILL.md + manifest.yaml), catálogo queries/performance/** (21 queries certificadas, Query Variant Resolver validado). Esta fila mide el core de performance no licenciado (ruta estándar sin Diagnostics Pack, Statspack) — genuinamente SUPPORTED 10g-23ai. AWR/ASH/ADDM son capacidades LICENSE_DEPENDENT independientes (filas propias abajo) — su disponibilidad real depende de licenciamiento del cliente, no de cobertura del e-stack. Ver docs/PHASE_3_ORACLE_PERFORMANCE.md."

  - id: awr
    display_name: "AWR"
    license_dependent: true
    versions: {10g: LICENSE_DEPENDENT, 11g: LICENSE_DEPENDENT, 12c: LICENSE_DEPENDENT, 18c: LICENSE_DEPENDENT, 19c: LICENSE_DEPENDENT, 21c: LICENSE_DEPENDENT, 23ai: LICENSE_DEPENDENT, latest: LICENSE_DEPENDENT}
    notes: "Requiere Diagnostics Pack en todas las versiones donde AWR existe (10g+). Ver policies/licensing-awareness-policy.md."

  - id: ash
    display_name: "ASH"
    license_dependent: true
    versions: {10g: LICENSE_DEPENDENT, 11g: LICENSE_DEPENDENT, 12c: LICENSE_DEPENDENT, 18c: LICENSE_DEPENDENT, 19c: LICENSE_DEPENDENT, 21c: LICENSE_DEPENDENT, 23ai: LICENSE_DEPENDENT, latest: LICENSE_DEPENDENT}
    notes: "V$ACTIVE_SESSION_HISTORY historico (DBA_HIST_ACTIVE_SESS_HISTORY) requiere Diagnostics Pack."

  - id: addm
    display_name: "ADDM"
    license_dependent: true
    versions: {10g: LICENSE_DEPENDENT, 11g: LICENSE_DEPENDENT, 12c: LICENSE_DEPENDENT, 18c: LICENSE_DEPENDENT, 19c: LICENSE_DEPENDENT, 21c: LICENSE_DEPENDENT, 23ai: LICENSE_DEPENDENT, latest: LICENSE_DEPENDENT}
    notes: "Depende de AWR + Diagnostics Pack. skills/performance/addm-analysis registered, no materializado."

  - id: statspack
    display_name: "Statspack"
    license_dependent: false
    versions: {10g: SUPPORTED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED, latest: SUPPORTED}
    notes: "Fase 3 Completion Hardening: performance/statspack-analysis ahora cubre reportes multi-sección completos vía parsers/performance/statspack_parser.py (Load Profile, Instance Efficiency, Top Wait Events, SQL ordered by CPU/elapsed/executions/gets/reads, Instance Activity, Library Cache, Latch, Enqueue, I/O incl. ASM, Memory/Cache Sizes, Redo/Commit y Parsing derivados), además de la vía de consulta en vivo (Q-PERF-WAIT-STATSPACK-001, sólo wait events). Cobertura por-sección es SUPPORTED/PARTIALLY_SUPPORTED/UNSUPPORTED según el reporte concreto suministrado — nunca se finge una sección ausente en el reporte fuente (ver skills/performance/statspack-analysis/SKILL.md#statspack-capability-map)."

  - id: rac
    display_name: "RAC"
    license_dependent: false
    versions: {10g: PLANNED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED, latest: SUPPORTED}
    notes: "Fase 4: RAC DEEP DIAGNOSTICS (topologia, servicios, session distribution, load balancing CLB/RLB, interconnect, Cache Fusion, recursos Clusterware) SUPPORTED desde 11gR2 (19 skills rac/* + 12 skills rac/gi-*). 10g/11gR1 (CRS legacy, sin Clusterware moderno) -> PLANNED, no UNSUPPORTED (la feature Oracle existe, este catalogo no la certifica). RAC DETECTION basica sigue SUPPORTED desde Fase 2 via el Target Profile. RAC deep performance (gc waits, impacto en DB Time) DELEGATES_TO_PERFORMANCE, no se duplica aqui."

  - id: gi
    display_name: "Grid Infrastructure"
    license_dependent: false
    versions: {10g: UNSUPPORTED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED, latest: SUPPORTED}
    notes: "Fase 4: GI absorbido por oracle-rac-analyst (sin agente separado), 12 skills rac/gi-* materializados (version, node-status, resource-status/-properties, scan, vip, listeners, network-interfaces, ocr-status, voting-status, cluster-health, configuration-consistency) -> SUPPORTED desde 11gR2. GI como marca/arquitectura formal es 11gR2+ (antes: Oracle Clusterware) -> UNSUPPORTED en 10g. OCR/Voting repair explicitamente PROHIBITED (solo visibility)."

  - id: asm
    display_name: "ASM"
    license_dependent: false
    versions: {10g: UNSUPPORTED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED, latest: SUPPORTED}
    notes: "Fase 4: ASM DEEP DIAGNOSTICS (topologia, diskgroups, capacidad real via USABLE_FILE_MB, redundancia, salud de discos, failure groups, rebalance) SUPPORTED desde 11gR2 (12 skills asm/*), reemplaza el asm/capacity.md plano de Foundation. 10g fuera de soporte activo (arquitectura de disk group difiere significativamente antes de 11g). ASM DETECTION basica sigue SUPPORTED desde Fase 2. ASM write operations explicitamente PROHIBITED."

  - id: multitenant
    display_name: "Multitenant"
    license_dependent: true
    versions: {10g: UNSUPPORTED, 11g: UNSUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED}
    future_status: COMPATIBILITY_VALIDATION_REQUIRED
    notes: "Fase 6: 26 skills multitenant/* completamente materializados (architecture, cdb-discovery, pdb-inventory, pdb-state, pdb-open-mode, pdb-services, pdb-rac-placement, pdb-sessions, pdb-tablespaces, pdb-temp, pdb-undo, local-undo, parameters, common-local-users, common-local-roles, components, plugin-violations, resource-usage, resource-manager, lockdown-profiles, application-containers, proxy-pdb, configuration-drift, healthcheck, assessment, troubleshooting), 15 queries Q-CDB-*/Q-CDB-PDB-* certificadas -> SUPPORTED 12.1-23ai para NON-CDB awareness, CDB discovery/root, PDB inventory/state/open-mode/services/RAC placement/sesiones/storage/temp/undo, parameter scope y drift, common/local identity (visibilidad), component health, plug-in violations, resource usage/manager (visibilidad), lockdown profiles (visibilidad), configuration drift. Local Undo NO existe en 12.1 (introducido en 12.2) -- degradado correctamente vía Query Variant Contract, nunca asumido. Application Containers / Proxy PDB (12.2+) son PARTIALLY_SUPPORTED (topología vía V\$PDBS, sin profundidad de lifecycle) -- ver agents/oracle-multitenant-analyst/manifest.yaml#supported_container_modes. Multitenant no existe antes de 12c -> UNSUPPORTED real (no límite del e-stack); en NON-CDB (cualquier versión) MULTITENANT_STATUS: NOT_APPLICABLE se publica directamente desde el Target Profile, sin activar el agente. Más de 1 PDB por CDB es LICENSE_DEPENDENT según edición (ver policies/licensing-awareness-policy.md) -- señalado por finding específico, no a nivel de dominio completo. CONTAINER DETECTION básica sigue SUPPORTED desde Fase 2 vía el Target Profile. El campo 'latest: SUPPORTED' fue eliminado de esta fila (mismo criterio que Data Guard Fase 5) -- una major Oracle nueva requiere /change compatibility antes de avanzar 'future_status' a SUPPORTED. Ninguna ejecución de ALTER PLUGGABLE DATABASE / CREATE-DROP-CLONE-UNPLUG-PLUG / SAVE STATE / cambio de parámetro / Resource Manager / lockdown profile / usuario común o local -- sólo visibilidad y diagnóstico. PHASE 6 — QUERY COMPATIBILITY & DICTIONARY CERTIFICATION HARDENING (sub-capacidades con precisión de versión distinta del agregado 12.1-23ai de la fila): PDB Saved State -> SUPPORTED desde 12.1.0.2 (patch level, no 12.1 genérico -- no existe en 12.1.0.0/12.1.0.1, vista real DBA_PDB_SAVED_STATES, no CDB_PDB_SAVED_STATES que no existe). PDB Resource Metrics -> 12.1: PARTIALLY_SUPPORTED (V\$RSRCPDBMETRIC no existe, introducida en 12.2.0.1, sin fuente alternativa certificada), 12.2+: SUPPORTED. Plug-in Violations -> SUPPORTED con variantes version-specific (12.1: sin CON_ID, container_id NOT_AVAILABLE; 12.2+: con CON_ID). Ninguna se marca con soporte superior al realmente certificado."

  - id: dataguard
    display_name: "Data Guard"
    license_dependent: false
    versions: {10g: SUPPORTED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED}
    future_status: COMPATIBILITY_VALIDATION_REQUIRED
    notes: "Fase 5: 21 skills dataguard/* completamente materializados (topology, role, protection, transport, apply, lag, archive-gaps, archive-destinations, processes, standby-redo-logs, real-time-apply, broker, fsfo, observer, switchover-readiness, failover-readiness, configuration-drift, healthcheck, assessment, troubleshooting, incident-analysis) -> SUPPORTED 10g-23ai para PHYSICAL_STANDBY (foco principal). LOGICAL_STANDBY/SNAPSHOT_STANDBY/FAR_SYNC reconocidos (rol/presencia) pero PARTIALLY_SUPPORTED -- analisis profundo especifico fuera de alcance de esta fase. Switchover/failover execution explicitamente PROHIBITED (solo readiness/visibility). Active Data Guard (lectura en standby) es LICENSE_DEPENDENT y se senala por finding especifico via ACTIVE_DATA_GUARD_CHECK, no a nivel de dominio completo. ROLE DETECTION basica sigue SUPPORTED desde Fase 2 via el Target Profile. PHASE 5 — DATA GUARD FINAL PROCESS-VIEW & PORTABILITY HARDENING (# 15-16 del prompt): la columna 'latest: SUPPORTED' fue ELIMINADA de esta fila (no sólo reinterpretada por comentario, como en el hardening anterior) -- reemplazada por el campo explícito 'future_status: COMPATIBILITY_VALIDATION_REQUIRED', consistente en una única interpretación con agents/oracle-dataguard-analyst/manifest.yaml#supported_versions (family 'future' -> status: UNKNOWN_FUTURE), config/query-compatibility-matrix.yaml (max explicito '23.0'/'12.1' segun variante, nunca 'latest', en todas las queries Q-DG-*), docs/CAPABILITY_MATRIX.md, y el Query Variant Resolver. Una major Oracle nueva requiere /change compatibility antes de avanzar cualquiera de estas fuentes a SUPPORTED."

  - id: rman
    display_name: "RMAN / Backup & Recovery"
    license_dependent: false
    versions: {10g: SUPPORTED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED}
    future_status: COMPATIBILITY_VALIDATION_REQUIRED
    notes: "Fase 7: 30 skills rman/* completamente materializados (configuration, backup-inventory, backup-status, backup-freshness, backup-duration, backup-throughput, full-backup, incremental-backup, archivelog-backup, controlfile-backup, spfile-backup, retention-policy, backup-optimization, channels, device-types, sbt-media-manager, fra, fra-pressure, obsolete-expired-awareness, snapshot-controlfile, rac-awareness, dataguard-awareness, multitenant-awareness, restore-readiness, recovery-readiness, pitr-readiness, pdb-pitr-awareness, troubleshooting, channel-contention, manual-recovery-plan) -> SUPPORTED 10g-23ai. Reemplaza el placeholder PARTIAL de Foundation (sólo skills/rman/backup-status.md materializado, plano). PDB PITR/multitenant-awareness quedan PARTIALLY_SUPPORTED como sub-capacidad (# 29 del prompt de Fase 7) — el resto del dominio no se ve afectado. RMAN execution (BACKUP/RESTORE/RECOVER/DELETE/CROSSCHECK/CHANGE/CONFIGURE/CATALOG/UNCATALOG/DUPLICATE/SWITCH DATABASE/ALLOCATE CHANNEL) explícitamente PROHIBITED — sólo visibilidad/readiness/runbooks manuales. PHASE 5/6 pattern seguido: 'latest: SUPPORTED' NUNCA declarado — 'future_status: COMPATIBILITY_VALIDATION_REQUIRED' explícito en su lugar, consistente con agents/oracle-backup-recovery-analyst/manifest.yaml#supported_versions y config/query-compatibility-matrix.yaml (max explícito, nunca 'latest', en las queries Q-RMAN-*)."

  - id: security
    display_name: "Security & Compliance"
    license_dependent: true
    versions: {10g: SUPPORTED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED}
    future_status: COMPATIBILITY_VALIDATION_REQUIRED
    notes: "Fase 8: 39 skills security/* completamente materializados (account-inventory, account-status, default-accounts, stale-accounts, common-local-users, roles, system-privileges, object-privileges, powerful-privileges, public-grants, admin-privileges, proxy-authentication, external-authentication, password-profiles, password-policy-strength, password-complexity, password-verify-function, password-verifiers, audit-configuration, unified-auditing, traditional-auditing, privileged-audit, tde-awareness, keystore-awareness, tablespace-encryption, network-encryption, tls-awareness, security-parameters, database-links, directories, database-vault-awareness, ols-awareness, data-redaction-awareness, data-masking-awareness, licensing-gates, compliance-mapping, security-healthcheck, security-assessment, manual-remediation-plan) -> SUPPORTED 10g-23ai para el dominio base (account/role/privilege/password posture, sin licensing dependency). Sub-capacidades con boundary propio degradan sin afectar el dominio completo: Unified Auditing PARTIALLY_SUPPORTED en 10g/11g (Traditional Auditing es la vía certificada ahí, verificado min 12.1 vía WebFetch/WebSearch); TDE/keystore/tablespace-encryption PARTIALLY_SUPPORTED en 10g/11.1 (V$ENCRYPTION_WALLET verificado min 11.2, column encryption sí disponible desde 10.2 vía DBA_ENCRYPTED_COLUMNS); INACTIVE_ACCOUNT_TIME PARTIALLY_SUPPORTED por debajo de 12.2 (verificado, no 12.1 pese a fuentes secundarias ambiguas); Database Vault/OLS/Data Redaction/Data Masking siempre LICENSE_DEPENDENT — nunca declarados SUPPORTED por mera visibilidad técnica, license.status default SEPARATELY_LICENSED/REQUIRES_REVIEW (# 39-43 del prompt de Fase 8). Password strength assessment es evaluación de POLÍTICA, nunca de contenido real de contraseñas — actual password inspection y password hash/verifier exposure explícitamente PROHIBITED, igual que password testing/cracking y toda ejecución de mutación (CREATE/ALTER/DROP USER|ROLE|PROFILE, GRANT/REVOKE, AUDIT/NOAUDIT, ADMINISTER KEY MANAGEMENT, cambios de Database Vault/OLS/Redaction/Masking). Mismo patrón que Fase 5/6/7: 'latest: SUPPORTED' NUNCA declarado — 'future_status: COMPATIBILITY_VALIDATION_REQUIRED' explícito, consistente con agents/oracle-security-analyst/manifest.yaml#supported_versions y config/query-compatibility-matrix.yaml (max explícito '23.0', nunca 'latest', en las 30 queries Q-SEC-*)."

  - id: network
    display_name: "Network"
    license_dependent: false
    versions: {10g: SUPPORTED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED, latest: SUPPORTED}
    notes: "Fase 4: 14 skills network/* completamente materializados (oracle-net, listeners, scan, scan-resolution, service-registration, local-listener, remote-listener, connection-path, tns-errors, timeouts, name-resolution, interconnect, healthcheck, troubleshooting), reemplaza el network/tns.md plano de Foundation. SCAN es exclusivo de RAC 11gR2+ pero el resto del dominio (listener, TNS errors, name resolution, connection path) aplica igual en Standalone 10g+. Oracle Net diagnostics SUPPORTED; ningún cambio de configuración de red/DNS/firewall/listener ejecutado (PROHIBITED)."

  - id: os
    display_name: "OS"
    license_dependent: false
    versions: {10g: SUPPORTED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED}
    future_status: COMPATIBILITY_VALIDATION_REQUIRED
    notes: "Fase 9: 45 skills os/* completamente materializados (discovery, platform-version, cpu-topology, numa, memory, swap, memory-pressure, hugepages, transparent-hugepages, process-limits, open-files, ulimits, systemd-limits, shared-memory, semaphores, aio, kernel-parameter-assessment, ephemeral-ports, tcp-socket-awareness, network-interfaces, bonding, vlan, mtu, routing, dns, filesystems, inodes, mount-options, block-devices, multipath-awareness, io-performance, time-sync, ssh-sshd-awareness, oracle-groups, oracle-processes, grid-processes, cgroups, log-pressure, rac-interconnect-awareness, dataguard-network-awareness, rman-media-manager-awareness, security-filesystem-awareness, platform-healthcheck, platform-assessment, manual-hardening-plan). No depende de la version Oracle -- 'os' es capa de plataforma, la fila se repite igual en las columnas por consistencia estructural con el resto de la matriz. Linux (Oracle Linux/RHEL/SUSE/LinuxONE) SUPPORTED, Windows Server SUPPORTED (con sub-capacidades PARTIALLY_SUPPORTED donde el modelo de OS difiere -- ulimits/hugepages/aio/mount-options NOT_APPLICABLE, ephemeral-ports/time-sync/network-interfaces/filesystems/process-limits/open-files SUPPORTED vía collectors WMI dedicados, ningun PowerShell arbitrario), Solaris PARTIALLY_SUPPORTED (comandos nativos propios -- psrinfo/prtconf/swap -s/ipadm/dladm show-aggr -- terminologia 'aggregation' en vez de 'bonding', nunca sintaxis Linux asumida). AIX/HP-UX quedan fuera de alcance de esta fase (registered en skills/REGISTRY.md, sin skill materializado -- ver docs/PHASE_9_OS_PLATFORM_DIAGNOSTICS_HARDENING.md). HugePages/kernel parameters siempre evaluados contra el sizing real del host (SGA/PROCESSES), nunca una cifra universal hardcodeada. Mutacion de OS (paquetes/servicios/kernel params/sysctl write/ulimits/usuarios-grupos/permisos/firewall/red/bonding/VLAN/mount/LVM-ZFS-VxVM/SELinux-AppArmor/sshd config/Windows Registry/Windows service/PowerShell arbitrario) explicitamente PROHIBITED -- solo visibilidad, manual hardening plan con execution_status NOT_EXECUTED. Deepening de agents/os-platform-analyst (manifest real desde Foundation, mismo patron que Security Fase 8) a contrato estructurado completo v2.0.0. Reemplaza el modelo platform-per-skill-id de Foundation (18 skills x 5 plataformas, solo os/linux/memory materializado) por un modelo domain-per-skill-id (os/<capability>, platform-awareness dentro de cada manifest via campo platforms:), igual que network/*/security/*. Reutiliza (nunca duplica) los collectors OS certificados desde Fase 4 (get_interfaces, get_routes, get_socket_summary, get_name_resolution, get_host_identity), propiedad de os-platform-analyst. Version futura: mismo criterio que Data Guard/Multitenant/RMAN/Security -- 'latest: SUPPORTED' NUNCA declarado, 'future_status: COMPATIBILITY_VALIDATION_REQUIRED' explicito en su lugar. PHASE 9 -- EFFECTIVE PROCESS LIMITS & SYSTEMD/PAM SOURCE-OF-TRUTH HARDENING (corrige un defecto real del build base: os/systemd-limits combinaba systemd y PAM/limits.conf mediante una regla universal de minimo, estructuralmente incorrecta para un proceso lanzado por systemd sin PAMName=): Effective process limits Linux SUPPORTED (get_process_effective_limits, PID-scoped via /proc/<pid>/limits), systemd limit awareness SUPPORTED (launch_context-aware, PAM ya no se asume aplicable sin evidencia), PAM limit awareness SUPPORTED (correlacionada solo cuando el launch_context lo confirma), cgroup pids/tasks awareness SUPPORTED (Linux)/PARTIALLY_SUPPORTED (Solaris, sin mecanismo equivalente certificado), Solaris effective process limits PARTIALLY_SUPPORTED (sin /proc/<pid>/limits equivalente certificado en esta fase, nunca simula paridad Linux), Windows effective process limits PARTIALLY_SUPPORTED (sin API de job objects/resource limits certificada todavia), Privilege escalation PROHIBITED (nunca sudo/su/runuser -- fallback INSUFFICIENT_PRIVILEGES + manual_collection). Ver docs/PHASE_9_EFFECTIVE_PROCESS_LIMITS_SYSTEMD_PAM_HARDENING.md. PHASE 9 -- PAM LIMITS APPLICABILITY & PROCESS CONSTRAINT SCOPE MICRO-HARDENING (corrige dos defectos introducidos por el hardening anterior: PAMName= tratado como prueba suficiente de PAM applicability sin verificar pam_limits.so, y os/cgroups combinando pids.max/nproc/TasksMax mediante un minimo universal): PAM session awareness SUPPORTED (PAMName= interpretado solo como sesion PAM solicitada, nunca como limits.conf applicability), pam_limits applicability SUPPORTED (Linux, get_pam_limits_applicability confirma pam_limits.so en la pila PAM efectiva, estados APPLICABLE/NOT_APPLICABLE/INSUFFICIENT_EVIDENCE/NOT_ASSESSED)/PARTIALLY_SUPPORTED (Solaris/Windows, sin mecanismo PAM equivalente certificado), PAM include-chain analysis SUPPORTED (Linux, max_depth/visited set contra loops), RLIMIT_NPROC SUPPORTED (scope USER), systemd TasksMax SUPPORTED (scope UNIT), cgroup pids.max SUPPORTED (Linux, scope CGROUP, requiere membership confirmada)/PARTIALLY_SUPPORTED (Solaris), binding-constraint analysis SUPPORTED (Linux, calculado desde scope+membership+uso+headroom, nunca min() universal)/PARTIALLY_SUPPORTED (Solaris/Windows). Ver docs/PHASE_9_PAM_LIMITS_PROCESS_CONSTRAINT_SCOPE_HARDENING.md. PHASE 9 -- PAM LIMITS POLICY SOURCE & PID CONTROLLER IDENTITY MICRO-HARDENING (corrige dos defectos introducidos por el micro-hardening anterior: applicability == APPLICABLE tratado como prueba de que limits.conf/limits.d eran la fuente efectiva sin parsear conf= real de pam_limits.so, y TasksMax/pids.max del mismo cgroup contados dos veces como constraints independientes): pam_limits default policy source SUPPORTED (Linux, mode DEFAULT con awareness de limits.conf/limits.d), pam_limits custom conf source SUPPORTED (Linux, get_pam_limits_policy_source detecta conf=<path>, tokenizado, nunca concurrente con la fuente default)/PARTIALLY_SUPPORTED (Solaris/Windows, sin PAM equivalente certificado), PAM module-argument parsing SUPPORTED (Linux, conf=/debug=/set_all=/utmp_early=, multiples invocaciones preservadas por separado), TasksMax -> pids.max mapping SUPPORTED (Linux, get_unit_cgroup_path resuelve unit_cgroup_path_token, nunca adivinado por nombre de servicio), PID-controller de-duplication SUPPORTED (Linux, constraint canonico PID_CONTROLLER cuando TasksMax y pids.max resuelven al mismo cgroup_path_token, nunca contados dos veces), cgroup hierarchy constraints SUPPORTED (Linux, relation UNIT/PARENT/CHILD/DELEGATED/OTHER, nodos distintos nunca deduplicados por similitud numerica)/PARTIALLY_SUPPORTED (Solaris/Windows). Ver docs/PHASE_9_PAM_POLICY_SOURCE_PID_CONTROLLER_IDENTITY_HARDENING.md."

  - id: capacity
    display_name: "Capacity"
    license_dependent: false
    versions: {10g: SUPPORTED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED}
    future_status: COMPATIBILITY_VALIDATION_REQUIRED
    notes: "Fase 10: 28 skills capacity/* completamente materializados (data-source-inventory, data-quality, normalization, cpu, memory, storage, oracle, asm, tablespace, os, windows, linux, sqlserver, vmware, horizontal, vertical, trend-analysis, forecasting, threshold-crossing, anomaly-awareness, seasonality-awareness, growth-rate, confidence, risk-classification, capacity-healthcheck, capacity-assessment, manual-capacity-plan, executive-summary), reemplaza el modelo de 16 skill_ids registered-only de Foundation (1 activo previo, capacity/forecast, absorbido en capacity/forecasting). CPU/Memory/Storage SUPPORTED transversal Linux/Windows Server (via os-platform-analyst, Fase 9, sin duplicar collectors). Oracle/ASM/Tablespace/FRA capacity SUPPORTED (via oracle-dba-analyst/oracle-asm-storage-analyst/oracle-backup-recovery-analyst, Fase 2/4/7, por referencia). Forecast 1/3/6 meses SUPPORTED (linear trend + confidence + reproducibilidad, nunca certeza). Threshold crossing SUPPORTED. Horizontal/vertical analysis SUPPORTED (siempre advisory, INSUFFICIENT_EVIDENCE cuando corresponda, nunca forzado por umbral de utilizacion). VMware capacity PARTIALLY_SUPPORTED (contract disenado, NOT_CERTIFIED por defecto -- MANUAL_IMPORT unicamente, sin collector runtime en esta fase). SQL Server capacity PARTIALLY_SUPPORTED (mismo criterio que VMware). No depende exclusivamente de la version Oracle -- capacity es dominio transversal, se repite igual en las columnas por consistencia estructural con el resto de la matriz (igual que 'os'). Capacity mutation execution (resize VM, add CPU/memoria, extender filesystem/ASM/tablespace, alterar asignacion VMware, cambiar parametros DB/OS, modificar herramientas de monitoreo) explicitamente PROHIBITED -- solo forecast/analisis/manual_action con execution_status NOT_EXECUTED. Un unico agente capacity-analyst transversal, nunca dividido por recurso/tecnologia. Mismo criterio que Data Guard/Multitenant/RMAN/Security/OS: 'latest: SUPPORTED' NUNCA declarado, 'future_status: COMPATIBILITY_VALIDATION_REQUIRED' explicito. PHASE 10 -- FORECASTING EXECUTION & NUMERICAL VALIDATION HARDENING: forecasting/data-quality/threshold-crossing pasan de CONTRACT_DEFINED a LOCAL_RUNTIME_TESTED via capacity_engine/ (Python stdlib puro, sin numpy/scipy/pandas) -- OLS real, agregacion diaria determinista, intervalos de prediccion, deteccion de resize/outliers/source-conflict, verificado con 18 tests numericos que ejecutan el motor (tests/test_capacity_engine_*.sh + test_forecast_linear_growth.sh/test_forecast_reproducible_same_input.sh endurecidos). /healthcheck capacity y /assessment capacity (orquestacion de agente vivo) permanecen CONTRACT_ONLY/NOT_RUNTIME_CERTIFIED -- dependen del MCP Gateway, fase posterior; capacity_engine.cli es el adaptador local LOCAL_RUNTIME_TESTED que demuestra el recorrido real sin ese runtime. Ver docs/PHASE_10_FORECASTING_EXECUTION_NUMERICAL_VALIDATION_HARDENING.md, docs/PHASE_10_CAPACITY_MANAGEMENT_FORECASTING.md."

  - id: incident
    display_name: "Incident Analysis & Root Cause Automation"
    license_dependent: false
    versions: {10g: SUPPORTED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED}
    future_status: COMPATIBILITY_VALIDATION_REQUIRED
    notes: "Fase 11: 34 skills incident/* completamente materializados (intake, classification, severity-awareness, scope-identification, timeline, evidence-plan, evidence-correlation, symptom-clustering, hypothesis-generation, hypothesis-testing, contradiction-analysis, root-cause, contributing-factors, impact-analysis, blast-radius, recovery-status, recurrence-awareness, known-error-correlation, change-correlation, capacity-correlation, performance-correlation, rac-correlation, dataguard-correlation, asm-storage-correlation, network-correlation, os-correlation, security-correlation, rman-correlation, multitenant-correlation, manual-remediation-plan, post-incident-review, lessons-learned, incident-report, rca-report), reemplaza el placeholder de 8 skill_ids de Foundation (1 activo previo, incident/root-cause-analysis, fusionado como base de incident/root-cause v2.0.0). Un único agente incident-root-cause-analyst transversal (v2.0.0), nunca dividido por capability — mismo criterio que capacity-analyst/os-platform-analyst. Intake/Triage/Timeline/Hypothesis/Root-Cause/Impact/Playbooks/Post-Incident Review SUPPORTED 10g-23ai (el modelo de incidentes no depende de la version Oracle; cada skill de correlacion cross-domain hereda el boundary de version/arquitectura del especialista que referencia — ej. incident/rac-correlation PARTIALLY_SUPPORTED en targets standalone via NOT_APPLICABLE, igual que rac). Root Cause Model estricto: CONFIRMED_ROOT_CAUSE requiere 2 fuentes de evidencia independientes o prueba temporal inequivoca, UNDETERMINED es estado terminal legitimo — nunca forzado. Correlacion temporal NUNCA equivale a causalidad (CHANGE_CORRELATED != CHANGE_CAUSED, distincion estructural en incident/change-correlation). Blast radius/impact nunca sobreestimados sin evidencia directa. Manual remediation ONLY — incluso acciones de emergencia (restart, relocate, kill session, failover, extender filesystem) quedan como MANUAL AUTHORIZED ACTION con execution_status NOT_EXECUTED, sin excepcion — mismo patron que change-advisor/capacity/manual-capacity-plan/os/manual-hardening-plan/rman/manual-recovery-plan/security/manual-remediation-plan. Cross-domain correlation reutiliza (nunca duplica) evidence_refs de los 10 especialistas previos (oracle-dba-analyst, oracle-performance-analyst, oracle-rac-analyst, oracle-asm-storage-analyst, oracle-dataguard-analyst, oracle-multitenant-analyst, oracle-backup-recovery-analyst, oracle-network-analyst, oracle-security-analyst, os-platform-analyst, capacity-analyst). Capacity forecast futuro NUNCA usado como prueba de causa de un incidente ya ocurrido sin evidencia historica/actual directa (incident/capacity-correlation). RMAN execution explicitamente PROHIBITED incluso durante investigacion de incidente (incident/rman-correlation nunca ejecuta RMAN). Mismo criterio que Data Guard/Multitenant/RMAN/Security/OS/Capacity: 'latest: SUPPORTED' NUNCA declarado, 'future_status: COMPATIBILITY_VALIDATION_REQUIRED' explicito. PHASE 11 — RCA EXECUTION & EVIDENCE VALIDATION HARDENING: incident/root-cause, incident/timeline, incident/hypothesis-generation, incident/hypothesis-testing, incident/contradiction-analysis pasan de CONTRACT_DEFINED a LOCAL_RUNTIME_TESTED via rca_engine/ (Python stdlib puro, sin numpy/scipy/pandas) -- motor de reglas real y determinista (intake/schema validation, normalizacion de timestamps a UTC con deteccion de clock skew, dedup con provenance, motor de hipotesis con reglas versionadas/allowlisted en rca_engine/rules/default_rules.json, gate de confirmacion que exige 2 fuentes independientes o prueba temporal inequivoca, contradicciones criticas que bloquean CONFIRMED, manifest de evidencia que bloquea CONFIRMED ante referencias rotas) -- verificado con 19 tests funcionales nombrados (tests/test_rca_*.sh) mas 1 control de mutation testing, todos ejecutando el CLI real (rca_engine.cli, LOCAL_RUNTIME_TESTED) contra fixtures sinteticos, nunca solo grep sobre Markdown. /diagnose incident, /rca, /healthcheck incident (orquestacion de agente vivo) permanecen CONTRACT_ONLY/NOT_RUNTIME_CERTIFIED -- dependen del MCP Gateway, fase posterior; rca_engine.cli es el adaptador local invocable que demuestra el recorrido real sin ese runtime, mismo patron que capacity_engine.cli en Fase 10. Ver docs/PHASE_11_RCA_EXECUTION_EVIDENCE_VALIDATION_HARDENING.md, docs/PHASE_11_INCIDENT_ANALYSIS_ROOT_CAUSE_AUTOMATION.md."

  - id: documentation
    display_name: "Documentation"
    license_dependent: false
    versions: {10g: PARTIAL, 11g: PARTIAL, 12c: PARTIAL, 18c: PARTIAL, 19c: PARTIAL, 21c: PARTIAL, 23ai: PARTIAL, latest: PARTIAL}
    notes: "PHASE 12 — CHANGE ADVISORY, DOCUMENTATION & KNOWLEDGE LIFECYCLE: 5 skills documentation/* nuevos o materializados (incident-rca-report, assessment-report, change-advisory-report, executive-summary, evidence-traceability) pasan a LOCAL_RUNTIME_TESTED via change_documentation_knowledge/ (Python stdlib, CLI `document`), verificados por tests/test_p12_documentation_factory.sh contra fixtures sinteticos y la salida real de rca_engine; el estado de causa es el de rca_engine (autoritativo), sin metricas inventadas, con reporte PARTIAL y warnings estructurados cuando faltan datos. Binarios DOCX/XLSX/PDF/PPTX siguen PLANNED (sin infraestructura probada). No depende de la version Oracle. skills/documentation/healthcheck-report.md materializado; entregables binarios (DOCX/XLSX/PDF/PPTX) quedan PLANNED, fase de implementacion aun no asignada (Fase 9 es OS Platform Diagnostics & Hardening, no documentacion -- correccion de una referencia obsoleta previa)."

  - id: change
    display_name: "/change"
    license_dependent: false
    versions: {10g: SUPPORTED, 11g: SUPPORTED, 12c: SUPPORTED, 18c: SUPPORTED, 19c: SUPPORTED, 21c: SUPPORTED, 23ai: SUPPORTED, latest: SUPPORTED}
    notes: "PHASE 12: 6 skills change/* nuevos (operational-advisory, impact-and-risk, compatibility-and-license-gates, manual-execution-plan, rollback-and-validation, stack-evolution-handoff) + dominio knowledge/* nuevo (7 skills: candidate-extraction, quality-gate, duplicate-and-conflict-review, version-and-provenance, review-and-approval, deprecation-and-retirement, retrieval) ejecutables via change_documentation_knowledge/ (LOCAL_RUNTIME_TESTED; CLI advise/kb-*), sin fila de dominio propia porque son procesos sobre artefactos ya saneados, no cobertura de version Oracle. Asesoria de cambio operativo = solo texto para ejecucion humana (NOT_EXECUTED_BY_ESTACK); gates capability/license/privilege/change_window UNKNOWN por defecto y jamas inferidos de la version; aprobacion/publicacion solo con registro de autorizacion humano externo (identidad no verificable por el e-stack). Orquestacion por runtime de agente vivo (/change, /document, /knowledge) permanece CONTRACT_ONLY hasta el MCP Gateway. El proceso de gobierno /change esta completo y probado (tests/test_change_governance_flow.sh) independientemente de la version del target -- es un proceso sobre el propio e-stack, no sobre el ambiente Oracle."
