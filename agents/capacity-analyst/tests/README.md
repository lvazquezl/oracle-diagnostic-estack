# Tests — capacity-analyst

Ver `tests/test_capacity_*.sh`, `tests/test_forecast_*.sh`, `tests/test_threshold_*.sh` en la raíz
del repositorio (convención del proyecto: todos los tests viven en `tests/`, no por agente). Este
directorio existe por consistencia estructural con el Agent Contract (mismo patrón que
`agents/os-platform-analyst/tests/README.md`).
