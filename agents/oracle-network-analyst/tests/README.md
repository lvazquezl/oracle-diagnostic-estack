# Tests — oracle-network-analyst

Los tests reales viven en el `tests/` plano de nivel repositorio. Tests de contrato: `tests/test_network_agent_manifest.sh`, `tests/test_network_agent_no_execution_capability.sh`. Tests de dominio: ver `docs/PHASE_4_RAC_GI_ASM_NETWORK.md#network-tests`.
