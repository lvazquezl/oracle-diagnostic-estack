# Tests — oracle-backup-recovery-analyst

Los tests de contrato de este agente viven en `tests/` (raíz del repositorio), no aquí — mismo patrón que `oracle-multitenant-analyst`/`oracle-dataguard-analyst`/`oracle-rac-analyst`/`oracle-asm-storage-analyst`/`oracle-network-analyst`.

Ver:

- `tests/test_backup_recovery_agent_manifest.sh`
- `tests/test_backup_recovery_agent_routing.sh`
- `tests/test_backup_recovery_agent_no_execution_capability.sh`
- `tests/test_backup_recovery_agent_no_delegation_loop.sh`
