# Context — ANA-YYYYMMDD-NNN

Output de `core/context-discovery` (`oracle-discovery-analyst`).

- Product version / edition:
- Instance mode (single/rac/rac_one_node):
- Container mode (non_cdb/cdb) / PDBs:
- Database role (primary/physical_standby) / Active Data Guard:
- Storage mode (asm/filesystem):
- Grid Infrastructure version:
- OS/platform (por nodo si RAC):
- Confidence: FACT|OBSERVATION|UNDETERMINED
- Evidence refs: []
