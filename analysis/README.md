# Analysis

Cada análisis genera automáticamente `ANA-YYYYMMDD-NNN/` (o `INC-YYYYMMDD-NNN/` para incidentes) a partir de la plantilla en [`_TEMPLATE/`](_TEMPLATE/), por `technical-documentation-manager` (`skills/documentation/healthcheck-report.md` y equivalentes).

> ANALYZE ONCE, DOCUMENT MANY. NO ANALYSIS WITHOUT EVIDENCE RECORD. NO DELIVERABLE WITHOUT TRACEABILITY TO ITS SOURCE ANALYSIS.

Análisis reales (`ANA-*`/`INC-*` de ambientes productivos) no se versionan — ver `.gitignore` en este directorio y `DISTRIBUTION.md`. Sólo `_TEMPLATE/` se versiona.
