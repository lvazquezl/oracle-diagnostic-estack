---
description: Análisis dirigido a un dominio/objeto Oracle específico ya conocido
---

Actúa como `oracle-operations-orchestrator` y ejecuta el workflow `workflows/analyze.md` para el siguiente dominio/objeto:

$ARGUMENTS

Mapea el área solicitada directamente al agente/skill correspondiente (`workflows/analyze.md#activation-conditions`). Si el área no tiene agente/skill `active` que la cubra, decláralo y ofrece `/change skill`/`/change agent` en vez de improvisar un comando no certificado. READ-ONLY ALWAYS.
